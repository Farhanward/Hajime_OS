# STATUS — Hajime OS

آخر تحديث: 2026-08-06

## أين وصلنا (2026-08-06، الأحدث)

**اثنا عشر مكوّناً، 403 اختبارات، صفر فشل.**

**المظهر صار جزءاً من النظام لا طبقة فوقه.** `hajime-brand` مصدر واحد: لوحة
ألوان، وهوية (المستودع والاسم ورابط الدعم)، وماسكوت مكتوب بالكود، ومولّد يُخرج
منها ثلاثة وثلاثين ملفاً — أربع شاشات بدقة 1920×1080، شعار المُقلِع بصيغتيه،
أيقونات ومفضلات، وملفات الثيم لكل سطح.

| يصل إلى | كيف |
|---|---|
| المُقلِع | `/boot/lua/gfx-hajime.lua` + `/boot/images/hajime-logo.png` |
| طرفية النواة | ست عشرة خانة `kern.vt.color` وخط 16×32 في `loader.conf` |
| نهاية الإقلاع | `rc.d/hajime_splash`: الماسكوت وأربعة فحوص حقيقية |
| الدخول | `/etc/motd.template` |
| سطح المكتب | خلفية، سبلاش، ثيم GTK، `wf-shell.ini`، مشغّلات `.desktop` |
| الكونسول | نفس اللوحة، الشعار مضمَّن، ومبدّل لغة |

**اللغة صارت إعداد نظام.** صفّا دخول `hajime-ar` و`hajime-en` في
`/etc/login.conf`، و`hajime-lang` يبدّل بينهما، و`Name[ar]` في كل مشغّل.
طرفية النواة تبقى إنجليزية لأن `vt(4)` لا يشكّل العربية ولا يعكس اتجاهها.

**ما لم يُختبر بعد:** لا شيء من هذا شُغِّل على الخادم. المثبّت يُحلَّل بـ `sh -n`،
والفحص النصي للأصول يمر في CI، لكن شاشة المُقلِع وألوان الطرفية لا تُرى إلا على
جهاز يُقلع فعلاً بـ FreeBSD، وهذا ما زال محجوزاً خلف نفس البوابة: الجهاز على
Alpine حتى الآن.

## أين وصلنا (2026-08-04، الأحدث سابقاً)

**أحد عشر مكوّناً، 373 اختباراً، صفر فشل.**

**النظام مُركَّب ويعمل على FreeBSD 14.4.** ليس تجربة جافة: المثبّت شُغِّل فعلاً،
والخدمة تعمل تحت `rc.d` بمستخدم غير مميّز، والكونسول يعرض الحالة الحيّة.

| المكوّن | الدور |
|---|---|
| `hajime-core` | مصادقة، سياسة، سجل، أسرار، إعداد، عقد الأدوات، **قفل** |
| `hajime-workflow` | 11 نوع عقدة، مجدول، خادم، `rc.d`، **وضع ظل** |
| `hajime-ai` | بوابة النموذج والأدوات |
| `hajime-wa` | بوابة واتساب + جسر Go |
| `hajime-social` | 4 أدوات نشر بمفاتيح حقيقية |
| `hajime-fetch` | جلب واستخلاص، بديل المتصفح |
| `hajime-sys` | `hajimectl`: خدمات، لقطات، فحص، توفير، **jails** |
| `hajime-console` | الصفحة الواحدة، بألوان الشعار |
| `hajime-wm` | سطح المكتب: wayfire وثيم ومثبّت |
| `hajime-jails` | ثلاثة jails، شبكة، قواعد pf |
| `hajime-model` | نموذج مُدرَّب من الصفر مجاله النظام: 176 كيلوبايت، 0.10 ثانية |

خمسة ملفات تنفيذية ومعها جسر Go.

### المثبّت والترحيل

- `install_hajime_os.sh` — قابل للتكرار، ينشئ بيئة إقلاع قبل أول تغيير،
  ويرفض نسخة منتهية الدعم أو قرصاً بلا ZFS. `--dry-run` يعرض الخطة كاملة.
- `hajime-migrate/backup.sh` — يعمل، أنتج نسخة 147 ميغابايت.
- `hajime-migrate/restore_data.sh` — يتحقق من المجاميع قبل أن يكتب بايتاً،
  ولا يبلّغ بنجاح لم يحدث.
- `hajime-jails/create_jails.sh` — مجموعة بيانات لكل jail ولقطة أساس.

### الثغرات الثلاث: أُصلحت

1. التشغيلات الفائتة تُكتشف وتُسجَّل، واللحاق اختياري.
2. قفل `flock` يمنع نسختين من تشغيل نفس الجداول.
3. وضع الظل في المحرّك: `POST /api/workflows/:id/run?shadow=1`.

### النموذج الأصلي

مُدرَّب من الصفر، بلا إطار وبلا أوزان منزَّلة. `world.rs` نموذج بارامتري بقيود
(فكرة CAD): يقول ما يفعله التغيير قبل حدوثه. `classify.rs` يفهم العربية
والإنجليزية معاً. `plan.rs` يحوّل النية غير المؤكدة إلى سؤال، ويملك نموذج العالم
حق النقض عليه.

دقة 0.778 على صياغات لم يرها (11 صنفاً، التخمين 0.09). الأهم أن ثقة الأخطاء
(0.43) أقل من ثقة الإصابات (0.67)، فالعتبة تحوّلها إلى أسئلة.

```bash
hajime-model ask "أوقف كادي"
```

وينفّذ عبر بوابة الأدوات نفسها، فكل نداء موسوم بأثره ومسجَّل في التدقيق
ومحجوز في وضع التجربة. خطة تحتاج موافقة لا تُنفَّذ تلقائياً.

### ما تأكّد بالتشغيل الفعلي

| | |
|---|---|
| البناء على FreeBSD | ستة ملفات تنفيذية، 37 دقيقة تحت المحاكاة |
| المثبّت | شُغِّل مرتين، وكتلة التنويمات بقيت **واحدة** |
| الخدمة | تعمل بمستخدم `hajime`، ملف pid في `/var/run/hajime/` |
| القفل | يرفض نسخة ثانية ويسمّي حاملها ووقته |
| المنافذ | 5678 للمحرك و8088 للكونسول، معاً بلا تصادم |
| الكونسول | 3786 بايت، ويعرض «25 MB of 6144 MB» من الآلة الحيّة |
| النموذج | `holdout 0.778` — مطابق لويندوز تماماً |
| دورة jail كاملة | إنشاء مجموعة بيانات، لقطة، تراجع، ورفض لقطة jail آخر |
| الاستعادة | ترفض نسخة تالفة، وترفض نسخة بلا `SHA256SUMS` |
| `--validate` | يقبل تصديراً سليماً ويرفض ملفاً معطوباً |

### ما تبقى

- **مرحلة الحزم وقواعد البيانات** — الجهاز الافتراضي بلا إنترنت، فلم تُختبر
  `pkg install` ولا تهيئة العنقود ولا تشغيل postgresql/mariadb/redis/caddy.
  على الخادم الحقيقي، وله إنترنت، تعمل المرحلتان طبيعياً
- تشغيل النظام على الخادم — يحتاج وسائط تثبيت وحضور فرحان
- تشغيل Wayland على العتاد — يحتاج حضوره
- بروفة الاستعادة — متجاوَزة بقراره

## أين وصلنا (2026-08-04)

**ستة صناديق، 210 اختبار، صفر فشل.** مُتحقَّق منها على FreeBSD 14.4 فعلياً.

| الصندوق | الحالة |
|---|---|
| `hajime-core` | مصادقة، سياسة، سجل، أسرار، إعداد، **عقد الأدوات** |
| `hajime-workflow` | 11 نوع عقدة، مجدول، خادم، `rc.d` |
| `hajime-ai` | بوابة النموذج + بوابة الأدوات + **جذر التركيب** |
| `hajime-wa` | الواجهة + جسر Go مبني ومختبَر على FreeBSD |
| `hajime-social` | 4 أدوات نشر، **مربوطة بالبوابة فعلياً** |
| `hajime-sys` | `hajimectl`: جدول الخدمات، اللقطات، الفحص الذاتي، وضع التوفير |

تشغيل فعلي أثبت أن النموذج يرى ثلاث أدوات نشر بمفاتيح حقيقية، ووضع الظل مفعّل.

### القدرات الأربع المطلوبة

مدير الحزم (`hajimectl`)، واللقطة قبل العمليات الخطرة (`bectl`/`zfs`)، والفحص
الذاتي قبل إعلان الجاهزية، ووضع التوفير الذي يوقف الاختياري ويُبقي المواقع.
**الأربع مبنية ومختبَرة.**

### النموذج الأصلي

مُدرَّب من الصفر، بلا إطار وبلا أوزان منزَّلة. `world.rs` نموذج بارامتري بقيود
(فكرة CAD): يقول ما يفعله التغيير قبل حدوثه. `classify.rs` يفهم العربية
والإنجليزية معاً. `plan.rs` يحوّل النية غير المؤكدة إلى سؤال، ويملك نموذج العالم
حق النقض عليه.

دقة 0.778 على صياغات لم يرها (11 صنفاً، التخمين 0.09). الأهم أن ثقة الأخطاء
(0.43) أقل من ثقة الإصابات (0.67)، فالعتبة تحوّلها إلى أسئلة.

```bash
hajime-model ask "أوقف كادي"
```

وينفّذ عبر بوابة الأدوات نفسها، فكل نداء موسوم بأثره ومسجَّل في التدقيق
ومحجوز في وضع التجربة. خطة تحتاج موافقة لا تُنفَّذ تلقائياً.

### ما تأكّد بالتشغيل الفعلي

| | |
|---|---|
| البناء على FreeBSD | ستة ملفات تنفيذية، 37 دقيقة تحت المحاكاة |
| المثبّت | شُغِّل مرتين، وكتلة التنويمات بقيت **واحدة** |
| الخدمة | تعمل بمستخدم `hajime`، ملف pid في `/var/run/hajime/` |
| القفل | يرفض نسخة ثانية ويسمّي حاملها ووقته |
| المنافذ | 5678 للمحرك و8088 للكونسول، معاً بلا تصادم |
| الكونسول | 3786 بايت، ويعرض «25 MB of 6144 MB» من الآلة الحيّة |
| النموذج | `holdout 0.778` — مطابق لويندوز تماماً |
| دورة jail كاملة | إنشاء مجموعة بيانات، لقطة، تراجع، ورفض لقطة jail آخر |
| الاستعادة | ترفض نسخة تالفة، وترفض نسخة بلا `SHA256SUMS` |
| `--validate` | يقبل تصديراً سليماً ويرفض ملفاً معطوباً |

### ما تبقى

- `hajime-fetch` (بديل المتصفح) — لم يبدأ
- `hajime-console` (لوحة تحكم النظام) — لم يبدأ
- `hajime-wm` (تنسيق wayfire وأصول البيكسل) — لم يبدأ
- المثبّت وjails والترحيل — لم يبدأ
- **ثلاث ثغرات مؤجلة بقرار:** التشغيلات الفائتة، القفل ضد التشغيل المزدوج،
  وضع الظل في محرك الـ workflow
- بروفة الاستعادة — **متجاوَزة بقرار فرحان**

## أين وصلنا (2026-08-03، الأحدث)

**أربعة أركان مبنية. 158 اختبار Rust، صفر فشل.**

| الركن | الحالة | الأسطر |
|---|---|---|
| `hajime-core` | مكتمل: auth, policy, history, secrets, config | 1085 |
| `hajime-workflow` | مكتمل: 11 نوع عقدة، مجدول، خادم، سجل | 3888 |
| `hajime-wa` | الواجهة مكتملة، الجسر مكتوب غير مترجَم | 653 + 364 Go |
| `hajime-ai` | بوابة النموذج + بوابة الأدوات | 1302 |

**المنصة: FreeBSD 14.4** (14.2 في الخطة الأصلية منتهية الدعم).

**الواجهة:** `wayfire` من حزمة جاهزة + `waybar` + تنسيق CSS وأصول بيكسل.

### قرار في `hajime-ai`

الواجهة الخارجية `/v1/chat/completions` بصيغة OpenAI لا صيغة Ollama، لأنها
المعيار الفعلي وتحمل استدعاء الأدوات كمفهوم أصيل. عقد Ollama بقي غلافاً
رقيقاً للتوافق أثناء الترحيل، ومعلَّم `deprecated` في `/status`.

**بوابة الأدوات** نقطة اختناق واحدة تفرض: ميزانية لكل أداة ولكل تشغيلة،
وقاطع دوران بعد فشل متكرر، ووضع ظل **مفعَّل افتراضياً** لكل أداة ذات أثر
خارجي، وسجل تدقيق لكل نداء. الأداة التي لا تعلن أثرها تُعامل كخارجية.

### ما لم يُتحقق منه بعد

- **جسر `whatsmeow`**: مكتوب ولم يُترجم. Go غير مثبت على جهاز التطوير.
- **بروفة استعادة النسخة الاحتياطية**: بوابة المرحلة صفر، ما زالت مفتوحة.
- **تشغيل Wayland على العتاد**: الوصفة معروفة ولم تُجرَّب.
- **خوادم MCP**: البوابة جاهزة لاستقبالها، ولم يُكتب أي خادم بعد.

## أين وصلنا (2026-08-03، مكتمل)

**`hajime-workflow` اكتمل.** الأنواع الأحد عشر كلها منفَّذة، ومعها المصادقة
وسجل التنفيذ الدائم وسكربت `rc.d`. 105 اختبارات، كلها ناجحة.

القدرات التي تلمس المضيف (`executeCommand`, `ssh`, `readWriteFile`) مبنية
لكنها **معطّلة افتراضياً**. السبب موثَّق في `decisions.md`: اثنان من workflows
المستوردة يمرّران جسم الـ webhook إلى الشل مباشرة.

الخدمة ترفض الربط خارج loopback بلا `HAJIME_TOKEN`، ونقاط التحكم كلها خلف
توكن، و`/health` وحدها مفتوحة للفحص الحي.

المرحلة التالية: بقية أركان السيرفر.

## أين وصلنا (سابقاً)

`hajime-workflow` صار خدمة عاملة. تُحمَّل الـ 17 workflow من تصدير n8n بلا
تحويل، وتُوجَّه الـ webhooks، وتُجدول المهام بتوقيت Asia/Riyadh.

قياس فعلي عند التشغيل بكل الـ 17: **25.3 MB RSS** (منها 4.6 MB خاصة)، مقابل
151 MB لـ n8n على السيرفر. رقم الخطة الأصلية كان 12 MB وهو غير محقَّق.

تشغيل حقيقي لـ Health Monitor عبر الواجهة: 1.3 ثانية، 4 عقد، ناجح.

74 اختباراً (70 وحدة + 4 تكامل)، كلها ناجحة.

**الناقص قبل أي نشر:** لا مصادقة على الواجهة (يربط على 127.0.0.1 فقط الآن)،
ولا تخزين دائم لسجل التنفيذ، ولا سكربت `rc.d`.

## أين وصلنا

جرد السيرفر وجرد n8n مكتملان. بدأ بناء محرك `hajime-workflow`: نموذج بيانات
متوافق مع n8n، ومحرك تنفيذ رسم بياني، وعقدة `httpRequest`. 15 اختباراً ناجحاً.

سكربت النسخ الاحتياطي لم يُكتب بعد، وهو بوابة المرحلة 0.

الخطة التنفيذية في `PLAN.md`، والجرد في `hajime-migrate/INVENTORY_2026-08-03.md`.

## أنواع العقد المطلوبة (من الجرد الفعلي)

17 workflow، 5 نشطة، 11 نوع عقدة متمايزاً فقط.

| النوع | مرات الاستخدام | الحالة |
|---|---|---|
| httpRequest | 23 | **منفَّذ** |
| code | 15 | يحتاج محرك JS |
| webhook | 8 | لم يبدأ |
| respondToWebhook | 7 | لم يبدأ |
| scheduleTrigger | 7 | لم يبدأ |
| executeWorkflowTrigger | 4 | لم يبدأ |
| manualTrigger | 2 | لم يبدأ |
| executeCommand | 2 | لم يبدأ |
| readWriteFile | 1 | لم يبدأ |
| ssh | 1 | لم يبدأ |
| rssFeedRead | 1 | لم يبدأ |

الـ 5 workflows النشطة تحتاج ستة أنواع: `scheduleTrigger`, `webhook`,
`executeWorkflowTrigger`, `httpRequest`, `code`, `rssFeedRead`.

راجعنا الكود الحالي بالكامل وبنيناه بـ `cargo check`. النتيجة: المشروع نموذج أولي، والتغطية الفعلية من مواصفات الخطة الأصلية بين 8 و12 بالمئة.

## الخطوة التالية

اعتماد `PLAN.md`، ثم بدء الجلسة 1: جرد السيرفر وكتابة سكربت النسخ الاحتياطي.

## العوائق

- ~~ذاكرة السيرفر غير معروفة~~ **مغلق:** 7.7 GB مؤكدة بالقياس، مع 4.3 GB سواب
  مستخدم. الميزانية في `PLAN.md` صحيحة، والضغط أشد مما قُدّر.
- **قرار مطلوب من فرحان:** الجرد أظهر أن n8n يستهلك 151 MB لا 350 MB، بينما
  حزمة Postiz تستهلك 1.05 GB. قرار استبدال n8n في الإصدار الأول يحتاج مراجعة.
- **حاجب:** لا يوجد نسخ احتياطي مثبت للسيرفر الحي. ممنوع أي عمل تدميري قبل الجلسة 2.
- **مفتوح:** نضج مكتبات Rust للـ DRM/KMS على FreeBSD غير مؤكد. يُحسم في الجلسة 3.

## حالة الكود الحالي

| Crate | يُترجم | يعمل فعلياً |
|---|---|---|
| `hajime-workflow` | نعم | لا. هيكل HTTP بدون منفّذ عقد |
| `hajime-wa` | نعم | لا. يرجّع نجاح ثابت بدون اتصال واتساب |
| `hajime-ai` | **لا** | لا. خطأ ترجمة + لا يوجد استدلال |
| `hajime-wm` | لا ينطبق | غير موجود. ملف CSS وسكربت فقط |
| `hajime-binary` | لا ينطبق | غير موجود إطلاقاً |

خطأ الترجمة: `hajime-ai/src/main.rs:14` حيث `ChatMessage` تشتق `Deserialize` فقط بينما تُستخدم داخل بنية تشتق `Serialize`.
