# progress — Hajime OS

يوميات مؤرّخة: ما حدث، ما تغيّر، ما التالي.

---

## 2026-08-03 (متأخر) — أول تشغيل حقيقي مقابل n8n

شُغّل `🚨 CarbonFlow — Hourly Health Monitor` المصدَّر من الإنتاج، بلا تعديل،
على المحرك. النتيجة قورنت ببيانات تنفيذ n8n رقم 2017 المخزّنة.

**ثلاثة أخطاء كشفها التشغيل الحقيقي ولم تكشفها اختبارات الوحدة:**

1. `httpRequest` كان يقرأ `url` حرفياً، بينما قيمته `"={{ $json.url }}"`.
   المعاملات تُقيَّم لكل عنصر في n8n. أُضيف `resolved()`.
2. `httpRequest` كان يشتغل مرة واحدة، بينما n8n يشغّله مرة لكل عنصر داخل.
   خمسة مسارات تعني خمسة طلبات. أُعيدت هيكلته حول `request_once`.
3. **خطأ في المحرك:** الـ workflow فيه مشغّلان يغذّيان نفس العقدة. قاعدة
   "انتظر كل المصادر" أوقفت التنفيذ إلى الأبد لأن المشغّل الآخر لن يعمل.
   المشغّلات مداخل بديلة لا نقاط دمج. أُضيف `reachable_from` فصار الانتظار
   على المصادر القابلة للوصول فقط.

**فرق في الوفاء صُحّح بالدليل:** كان المخرَج `{statusCode, body}`. فحص بيانات
n8n أظهر عدم وجود مفتاح `statusCode` إطلاقاً، فـ n8n يُخرج جسم الرد مباشرة.
صار الغلاف اختيارياً عبر `options.fullResponse`.

**تطابق مؤكَّد:** `checked_paths` يعطي `unknown` خمس مرات عندنا وعند n8n سواء.
كلمة `unknown` موجودة نصاً في بيانات تنفيذ n8n. الـ workflow فيه خلل كامن
(المسارات تُفقد لأن `httpRequest` يستبدل العنصر)، والمطابقة أهم من التحسين
أثناء الترحيل. مثبَّت باختبار.

الحالة: 58 اختبار وحدة + 4 تكامل، كلها ناجحة. التشغيل الحي 1.2 ثانية.

---

## 2026-08-03

**ما حدث**

مراجعة كاملة للمشروع القائم مقابل `implementation_plan.md`.

بناء فعلي بـ `cargo check --workspace`. النتيجة: `hajime-ai` لا يُترجم بسبب `E0277` في `main.rs:27`، بينما `hajime-workflow` و`hajime-wa` يُترجمان.

مسح المستودع: صفر اختبارات، لا README، لا `.gitignore`، لا `Cargo.lock`، لا CI، لا مستودع git. إجمالي 339 سطر Rust.

فحص أمني كشف: كل الخدمات تسمع على `0.0.0.0` بلا مصادقة، `CorsLayer::permissive()` على الكل، وثغرة SSRF في `extract_media_bytes` تقبل أي رابط بلا قائمة سماح ولا حد لحجم التحميل.

فحص `restore_data.sh` كشف أنه يطبع "0 Data Loss" مهما فشلت العمليات، لأن كل أمر عليه `|| true`.

قياس فعلي لملف الانترو: 2,752,476 بايت، 1280×720، 24 إطار/ث، عشر ثوانٍ.

**ما تغيّر**

كُتبت `PLAN.md` بثمانٍ وعشرين جلسة عبر سبع مراحل.
أُنشئت ملفات الذاكرة الأربعة.
صُحّح ترتيب الترحيل بعد اكتشاف أن الترتيب المطروح يضع المسح قبل النسخ الاحتياطي.

**قرارات أخذها فرحان**

Wayland بتركيب كامل، سبرايتات بيكسل آرت، طبقة CRT مطلوبة، تسريع عتادي مؤجل، دقة 1080p، نظام أصلي لا مركّب.

**ما التالي**

اعتماد `PLAN.md`، ثم الجلسة 1: جرد السيرفر وسكربت النسخ الاحتياطي.

**غير محسوم**

ذاكرة السيرفر الفعلية. الميزانية الحالية مبنية على 8GB وتُظهر استهلاكاً حوالي 6.9GB، وهامش أقل من جيجابايت لا يكفي. اقتُرحت إضافة 16GB كأرخص حل.

---

## 2026-08-04 — المثبّت والاستعادة وjails والثغرات الثلاث

**ما اكتُشف**

`install_hajime_os.sh` القديم كان يستهدف 14.2 (منتهية الدعم)، ويُلحِق بـ
`loader.conf` بـ `cat >>` فتشغيله مرتين يضاعف كل سطر، ويضبط تنويمات
`hw.re.tso` و`hw.re.rxcsum` **غير الموجودة أصلاً** في `re(4)`، ويثبّت ثلاثة من
خمسة ملفات تنفيذية.

`restore_data.sh` القديم كان ينهي كل أمر بـ `|| true` ثم يطبع «0 Data Loss».
استعادة فاشلة تُبلَّغ كنجاح.

**خطأ حقيقي كشفه الاختبار على الهدف:** الثلاثة `rc.d` تضع ملف الـ pid في
`/var/run` مباشرة، لكن `rc.subr` يشغّل الأمر عبر `su -m ${name}_user`
(السطر 1163، قرأته على FreeBSD نفسه)، و`/var/run` هو `drwxr-xr-x root:wheel`.
جرّبت الكتابة بمستخدم غير مميّز فرُفضت. الخدمات كانت ستفشل في القيام، أو تقوم
بلا ملف pid فلا يجد `service stop` ما يشير إليه. نُقل إلى `/var/run/hajime/`
يُنشأ في `prestart` الذي يعمل قبل الـ `su`.

`wayland_feasibility_test.sh` كان كله CRLF فلا يُحلَّل على FreeBSD. وفحص
الـ CRLF الذي كتبته داخل الجهاز الافتراضي مرّ عبر ثلاث طبقات اقتباس **وأبلغ
بالسلامة فوق ملف معطوب بالكامل**. فحص لا يستطيع أن يفشل أسوأ من لا فحص، فأُعيد
كتابته ليعمل على البايتات محلياً.

**ما بُني**

مثبّت جديد: قابل للتكرار (كتلة معلَّمة تُستبدل)، وقابل للتراجع (بيئة إقلاع قبل
أول تغيير)، وصادق (لا `|| true`). تجربته الجافة لا تتوقف عند أول عائق بل تعرض
الخطة كاملة.

استعادة جديدة: تتحقق من `SHA256SUMS` قبل أن تكتب بايتاً، وتستخدم
`ON_ERROR_STOP=1` مع psql، وترفض الكتابة فوق قواعد موجودة بلا `--force`،
وتفحص الوركفلوهات بالمحرّك الذي سيشغّلها.

ثلاثة jails على مجموعات بيانات منفصلة، مع قواعد pf تمنع النموذج من الوصول
للخارج بنفسه. تحقّقت أن `jail.conf` يُحلَّل فعلاً على FreeBSD بدل افتراض ذلك.

**الثغرات الثلاث: أُصلحت**

النبضة تُكتب على القرص، فالإقلاع يعرف ما فاته ويسجّله. قفل `flock` يمنع نسختين.
وضع الظل في المحرّك مع إعلان أثر لكل عقدة، والافتراضي الأخطر.

**ما التالي**

التثبيت الفعلي على الخادم. يحتاج وسائط تثبيت وحضور فرحان: الجهاز بلا IPMI،
وFreeBSD لا تُثبَّت عبر SSH.

**غير محسوم**

الذاكرة ما زالت 8GB. `hajimectl save` يخفف الضغط لكنه لا يلغي الحاجة.

---

## 2026-08-05 — نموذج من الصفر

فرحان طلب نموذجاً «وظيفته النظام و تركيبته»، وأشار إلى ADK وإلى برمجة النماذج
في الـ CAD.

**قلت التحفّظ صراحةً:** نموذج لغوي عام من الصفر على هذا العتاد غير ممكن. لكن
المطلوب لم يكن عاماً. نموذج ضيق المجال: 0.10 ثانية تدريباً، 176 كيلوبايت.

**فكرة CAD طُبِّقت حرفياً:** كيانات وقيود وحلّال يجيب قبل التغيير لا بعده.

**أربعة أخطاء حقيقية كشفها القياس:** التدريب لم يكن يتدرّب أصلاً (تقسيم الخطوة
على عدد الأمثلة)، والتطبيع العربي لا يطوي ئ/ؤ، و«ال» تخفي اسم الخدمة، وأمر
`jail-snapshot` يُصدَر لخدمة ليست jail.

**واختبارٌ أمسك تلوّثي:** عند توسيع المتن نسختُ جملاً من مجموعة الاختبار إلى
التدريب، فقفز الرقم إلى 0.844 كذباً. الصادق **0.778**.

**ما التالي:** ربط النموذج بالكونسول وبالبوابة، وتشغيل الجهاز الافتراضي مجدداً
(سقط أثناء العمل) لتأكيد البناء على FreeBSD.

---

## 2026-08-05 — النظام مُركَّب ويعمل على FreeBSD

**ما تأكّد:** بناء ستة ملفات تنفيذية (37 دقيقة)، ومثبّت شُغِّل **مرتين** فبقيت
كتلة التنويمات واحدة، وخدمة تعمل بمستخدم غير مميّز وملف pid في مكان يملكه،
وقفل يرفض النسخة الثانية ويسمّي حاملها، ومنفذان منفصلان بلا تصادم، وكونسول
يعرض «25 MB of 6144 MB» من الآلة الحيّة. **372 اختباراً، صفر فشل.**

**خطأ حقيقي كشفه التشغيل:** `Common::from_env` كان يرجع افتراضياً إلى منفذ
محرك الوركفلو **لكل** ركن. الكونسول يوثّق 8088 ويربط على 5678. لم يكن أي اختبار
ليمسكه لأن التوثيق والسلوك اختلفا بصمت. الافتراضي صار وسيطاً، وأُضيف اختبار
يمنع ركنين من الوقوع على منفذ واحد.

**عوائق البيئة، لا النظام:** الجذر UFS تلف بعد إطفاء غير نظيف فسقط إلى المستخدم
الواحد (أُصلح بـ fsck عبر مقبس المراقبة)، وأرشيف crate مبتور أوقف البناء،
والجهاز بلا وصول للإنترنت فنُقلت 544 أرشيف crate من المضيف — وهنا لزم
`cargo fetch --target x86_64-unknown-freebsd` لأن بناء ويندوز لا يجلب كريتات
يونكس أبداً.

**ما تبقى:** مرحلة الحزم وقواعد البيانات (تحتاج إنترنت)، والتثبيت على الخادم.

---

## 2026-08-05 — اختبار وظيفي، وخطآن حقيقيان

اختبرت ما لا تصله اختبارات الوحدة: دورة jail كاملة على حوض ZFS حقيقي، وسكربت
الاستعادة على نسخة حقيقية، و`--validate`.

**خطأ ١:** أسماء مجموعات البيانات مثبَّتة على `zroot`. صحيحة على تركيب FreeBSD
افتراضي، وخاطئة **بصمت** على أي غيره: كل jail «لم يُنشأ» ولا تراجع يجد شيئاً.
ظهر لأن حوض الجهاز اسمه `hajimepool`. الحوض يُقرأ الآن من الآلة.

**خطأ ٢:** فحص المساحة يطلب حصة ثلاثة jails مهما طلبت. `--only ai` كان يُرفض
على آلة تتسع لواحد.

**ونتيجتان بدتا خطأً ولم تكونا:** نمط فحص لوحة الكونسول كان يتجاهل أن الفاصلة
العليا تُهرَّب إلى `&#39;`؛ ونسختي الاصطناعية أدرجت `SHA256SUMS` في قائمته
الخاصة بينما `backup.sh` الحقيقي يكتبه خارجها. الخلل في اختباراتي، والنظام
سليم في الحالتين.

**تأكّد بالتشغيل:** لقطة jail ثم تراجع إليها، ورفض لقطة تخصّ jail آخر،
وفحص مساحة يتناسب مع الطلب (4 جيجابايت لواحد، 12 لثلاثة).
