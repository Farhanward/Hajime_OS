# Hajime OS — تصنيف الأدوات وتصميم النظام

آخر تحديث: 2026-08-03
المنصة المعتمدة: FreeBSD 14.4. المصادر في آخر الملف.

---

## 1. تصنيف الأدوات حسب دعم FreeBSD

### أخضر: تُثبَّت مباشرة، بلا تعديل

| الأداة | المنفذ | الدور |
|---|---|---|
| PostgreSQL 17 | `databases/postgresql17-server` | قاعدة بيانات |
| MariaDB 11.4 | `databases/mariadb114-server` | قاعدة المتجر |
| Redis | `databases/redis` | ذاكرة وطوابير |
| Caddy | `www/caddy` | بروكسي عكسي وTLS تلقائي |
| cloudflared | منفذ رسمي منذ 2019 | النفق |
| llama.cpp | يُبنى من المصدر، ويعمل داخل jail | محرك الاستدلال |
| Ollama | `misc/ollama` | غلاف llama.cpp، اختياري |
| Node.js | `www/node` | لازم فقط إن أبقينا n8n محرراً |
| PHP + Apache | منافذ رسمية | LiteCart |
| Bastille | `sysutils/bastille` | إدارة الـ jails |
| ZFS | في النظام الأساسي | التخزين واللقطات |
| whisper.cpp | يُبنى من المصدر | تفريغ الصوت |
| **Stalwart** | **دعم FreeBSD رسمي** | خادم البريد |

ZFS و jails ليسا إضافة، هما في النظام الأساسي. هذا الفرق الجوهري عن لينكس.

### أصفر: يعمل بتحفّظ

| الأداة | التحفّظ |
|---|---|
| n8n | يعمل عبر npm لأن Node مدعوم، لكن n8n نفسه لا يدعم FreeBSD رسمياً |
| uptime-kuma | نفس الحالة |
| CrowdSec | المنفذ موجود، ويحتاج تحققاً عملياً |
| Ollama | يعمل، لكن تسريع كرت الشاشة محدود. llama.cpp مباشرةً أفضل |

### أحمر: لا مسار على FreeBSD، نبنيها بأنفسنا

| الأداة الحالية | لماذا | البديل |
|---|---|---|
| **Lightpanda** | Zig، ثنائيات لينكس وماك فقط | `hajime-fetch` |
| **cloakbrowser** | صورة Docker مخصصة | `hajime-fetch` |
| **WAHA** | Docker حصراً | `hajime-wa` |
| **Activepieces** | Docker أولاً | `hajime-workflow` (مبني) |
| **nginx-proxy-manager** | Docker حصراً | Caddy يغني عنه |
| **Postiz + Temporal + ES** | Docker وNode | `hajime-workflow` + خادم نشر MCP |
| **صورك المخصصة** | control-panel، release-api، telemetry-api، fatmalens، qood | تُعاد كتابتها بـ Rust |

**اعتراف:** رشّحت Lightpanda دون فحص المنصة. ثنائياتها للينكس وماك فقط، ولا ذكر لـ FreeBSD في بنائها. الترشيح كان خاطئاً.

---

## 2. ما نبنيه بـ Rust

### `hajime-workflow` — مكتمل
105 اختبارات، 25.3 ميجا، يشغّل الـ 17 workflow.

### `hajime-core` — التالي
المصادقة والسياسة والسجل والأسرار وMCP وبوابة الأدوات ووضع الظل.

### `hajime-wa` — بوابة واتساب
جسر `whatsmeow` عبر عملية Go. Go يدعم FreeBSD رسمياً.

### `hajime-fetch` — بديل المتصفح، بثلاث طبقات

بناء متصفح كامل من الصفر ليس واقعياً. Lightpanda استغرقت فريقاً سنوات. لكن
معظم ما تحتاجه لا يتطلب متصفحاً:

| الطبقة | ما تفعله | الجهد |
|---|---|---|
| ١ | جلب HTTP، تحليل HTML، استخلاص النص المقروء | أيام |
| ٢ | تنفيذ JavaScript البسيط عبر QuickJS (موجود عندنا في عقدة `code`) | أسابيع |
| ٣ | DOM كامل وCDP وتصيير | **غير واقعي** |

الطبقتان الأولى والثانية تغطيان الجلب والاستخلاص والمواقع الخفيفة. المواقع
التي تحتاج تصييراً كاملاً تبقى فجوة معروفة، وحلّها عند الحاجة متصفح بعيد لا
بناء محرك.

هذا صدق ضروري: لا أستطيع أن أعدك بمتصفح.

### `hajime-ai` — بوابة النموذج
مضيف MCP يجمع الأدوات، ومحرك الاستدلال في عملية منفصلة (llama.cpp).

### خوادم MCP الصغيرة
كل قدرة خادم مستقل: النشر الاجتماعي، البريد، التفريغ الصوتي، OCR، الجلب.

---

## 3. ما أريده لو كان النظام لي

### الأولوية الأولى: التراجع عن أي شيء

**بيئات إقلاع ZFS.** هذه أقوى ميزة في FreeBSD ولم تذكرها الخطة الأصلية
إطلاقاً. قبل أي ترقية أو تغيير: `bectl create` ثم طبّق التغيير. انكسر شيء؟
تعيد الإقلاع على بيئة الأمس. النظام كله يرجع، لا ملف واحد.

هذه وحدها تبرر اختيار FreeBSD أكثر من أي حجة أخرى في الخطة.

ومعها: jail لكل خدمة، ولقطة ZFS لكل jail. تحدّث خدمة واحدة وترجعها وحدها
دون لمس البقية.

### الأولوية الثانية: يعمل شهراً بلا لمسة

الخدمات تقوم مع النظام عبر `rc.d`. عطل واحد لا يسقط البقية. وعند الفشل يصلني
تنبيه بقناة أثق بها، لا صفحة أراجعها إن تذكّرت.

### الأولوية الثالثة: صفحة واحدة تقول كل شيء

أفتح عنواناً واحداً فأرى: ما المكسور، وما الذي عمل اليوم، وما القادم، وماذا
فعل النموذج ولماذا. لا تجميع من خمس واجهات.

### الأولوية الرابعة: النموذج لا يستطيع إيذائي

سقف لكل أداة، وقاطع عند التجاوز، ووضع ظل افتراضي لكل ما يخرج للعالم، وسجل
تدقيق يجيب: من أرسل ماذا ومتى وبأي تفويض.

### الأولوية الخامسة: بياناتي حرة

صيغ مفتوحة، ونسخ مجرَّبة الاستعادة لا موثوقة بالظن، ولا رخصة تقيّد ما أفعله
بنظامي.

---

## 4. الواجهة: إعادة نظر توفّر أشهراً

الخطة الأصلية تريد بيئة سطح مكتب: مدير نوافذ Wayland مركّب، بيكسل آرت،
عدادات تناظرية. قدّرت لها أشهراً.

لكن هذا **سيرفر بلا شاشة**. من سيرى سطح المكتب؟

**الاقتراح: الواجهة الريترو تصير كونسول ويب لا سطح مكتب.**

| | مدير نوافذ Wayland | كونسول ويب |
|---|---|---|
| الجهد | أشهر | أسابيع |
| الذاكرة | 80 إلى 130 ميجا | ~10 ميجا |
| الوصول | أمام الجهاز فقط | من أي جهاز، ومن الجوال |
| البيكسل آرت | ممكن | ممكن تماماً |
| متصفح ملفات | ممكن | ممكن |
| اختبار الجدوى على FreeBSD | مطلوب وغير منفَّذ | غير مطلوب |

النتيجة المرئية واحدة: إطارات CRT، وأيقونات بيكسل، وشريط مهام، ومتصفح ملفات
بنفس الطابع، وشعارك الكريمي والكحلي والوردي بدل السماوي البارد الذي في
`hajime_theme.css` الحالي والمتعارض مع شعارك.

والانترو يبقى: يُعرض عند فتح الكونسول لا عند إقلاع الجهاز.

`hajime-wm` يبقى في الخطة لكن أخيراً، ولمن يجلس أمام الجهاز فعلاً.

---

## 5. ما أضيفه من عندي

**مدير حزم للنظام نفسه.** أمر واحد يعرف كل خدمات Hajime ويشغّلها ويوقفها
ويحدّثها ويعرض حالتها. بدونه ستتعامل مع تسع خدمات بتسع طرق.

**لقطة قبل كل عملية خطرة.** أي ترحيل أو ترقية يأخذ لقطة ZFS تلقائياً باسم
مفهوم. الرجوع يصير أمراً واحداً لا عملية إنقاذ.

**فحص ذاتي عند الإقلاع.** النظام يتحقق أن كل خدمة تسمع على منفذها، وأن النفق
متصل، وأن الشهادات صالحة، وأن النسخة الاحتياطية جرت أمس. ويعلن النتيجة قبل
أن يعلن أنه جاهز.

**وضع التوفير.** أمر يوقف ما ليس ضرورياً (النموذج، التفريغ، الجلب) ويترك
المواقع تعمل. مفيد على 8 جيجا حين تحتاج ذاكرة فجأة.

---

## المصادر

- cloudflared على FreeBSD: <https://micahyeager.com/posts/using-cloudflared-with-freebsd-and-pfsense>
- llama.cpp في jail: <https://forums.freebsd.org/threads/llama-cpp-ai-working-in-a-jail-using-nvidia-gpu.102121/>
- منفذ Ollama: <https://cgit.freebsd.org/ports/tree/misc/ollama/Makefile>
- Stalwart يدعم FreeBSD: <https://deepwiki.com/stalwartlabs/stalwart/1.1-installation-and-deployment>
- Lightpanda لينكس وماك فقط: <https://github.com/lightpanda-io/browser>
- Docker لا يعمل على FreeBSD: <https://oneuptime.com/blog/post/2026-02-08-how-to-install-docker-on-freebsd-using-linux-emulation/view>
- Jails مقابل Docker: <https://freebsdsoftware.org/blog/freebsd-jails-vs-docker/>
