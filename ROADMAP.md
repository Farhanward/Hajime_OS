# الطريق من جهاز يعمل إلى نظام يُوزَّع

آخر تحديث: 2026-08-10، بعد أول تثبيت حقيقي على عتاد.

هذا الملف عن المستقبل. الماضي في موضعين: `hajime-migrate/FIRST_INSTALL.md`
لخمسة عشر عيباً كشفها التشغيل وكيف أُصلحت، و`decisions.md` لسبب كل قرار غير
بديهي. لا تُنسخ محتوياتهما هنا.

---

## أين نحن، مقيساً لا مقدَّراً

كل سطر أدناه قُرئ من الجهاز يوم 2026-08-10، لا من نيّة.

| | |
|---|---|
| النظام | FreeBSD 14.4-RELEASE على ZFS، مجموعة `zroot` سعة 460 GB |
| العتاد | Lenovo، i7-7700، 7.9 GB ذاكرة، SSD 512 GB، شبكة Realtek `re0` |
| التراجع | بيئتا إقلاع، إحداهما حالة ما قبل التثبيت |
| الخدمات العاملة | postgresql، mysql، redis، caddy، الكونسول على 8088، hajime_wa |
| سطح المكتب | يقلع وحده: getty بوسم `hajime-al` ثم wayfire وwf-panel وwf-background |
| الدقة | 1920x1080 من لحظة الإقلاع |
| الثيم | `verify_theme.sh` يقول: لا شيء ناقص |
| مسارات العمل | التصدير الحقيقي يُقرأ كاملاً: 17 من 17، وأنواع العقد الأحد عشر كلها مسجَّلة |
| الاختبارات | 409 تمر، وclippy نظيف، وCI يبني ويشغّل على FreeBSD 14.4 حقيقية |

وما لم يُنفَّذ ولا مرة: استعادة قواعد البيانات على بيانات حقيقية. قرار مسجَّل
لا سهو، والنسخة الكاملة للخادم القديم (34 GB، مُتحقَّق منها) هي شبكة الأمان.

---

## أربعة تمنع أي شخص غيرك

هذه ليست تحسينات. كل واحد منها يعني أن الثاني الذي يثبّت النظام يقابل جهازاً
لا يعمل، ولا شاشة بعيدة ليصلحه بها.

### 1. الشبكة مثبّتة على عنوان واحد

`rc.conf` يحمل `ifconfig_re0="inet 192.168.100.59 netmask 255.255.255.0"`. من
يثبّته في شبكة أخرى يحصل على تعارض عنوان أو جهاز بلا مسار.

**المطلوب**: DHCP افتراضاً، والعنوان الثابت خياراً يُسأل عنه مرة. واسم الواجهة
يُكتشف لا يُفترض، فليست كل بطاقة `re0`.

**تمّ حين**: يُثبَّت على جهاز في شبكة مختلفة فيحصل على عنوان ويُرى من الشبكة،
بلا تحرير ملف.

### 2. تعريف كرت الشاشة مختار لجهاز واحد

`i915kms` صحيح لبطاقة Intel هنا وحدها. AMD تحتاج `amdgpu`، وNVIDIA تحتاج
`nvidia-driver`، والقديمة تحتاج `radeonkms`. الخطأ هنا شاشة سوداء بعد أول
إقلاع.

**المطلوب**: اكتشاف البائع من `pciconf` واختيار الوحدة والبرنامج الثابت
المطابقين، مع تراجع إلى `scfb` العام حين لا يُعرف الكرت. وشاشة نصية تعمل دائماً
أفضل من سطح مكتب لا يظهر.

**تمّ حين**: يُثبَّت على جهاز بكرت غير Intel فيصل إلى سطح المكتب، أو يصل إلى
طرفية تعمل ويقول أي تعريف لم يجد.

### 3. لا جدار ناري إطلاقاً

`pf_enable=NO`، و`/dev/pf` غير موجود أصلاً أي أن الوحدة لم تُحمَّل قط. خادم
يخدم مواقع بلا مرشّح واحد.

**المطلوب**: مجموعة قواعد `pf` افتراضية: كل شيء صادر مسموح، والوارد مرفوض عدا
ssh والمنافذ التي تعلنها الخدمات في `service.rs`. ومصدرها ذلك الجدول لا ملف
ثانٍ يفترق عنه.

**تمّ حين**: `pfctl -s rules` يُظهر قواعد مولَّدة من جدول الخدمات، ومنفذ خدمة
معطّلة مغلق فعلاً.

### 4. لا طريق للتحديث

لا `freebsd-update` مجدول، ولا `pkg upgrade`، ولا شيء يخبر أحداً أن ترقيعاً
أمنياً صدر. توزيعة بلا قصة تحديث ليست توزيعة.

**المطلوب**: فحص مجدول يقول ما الجديد ولا يثبّته وحده، و`hajimectl update`
يثبّته داخل بيئة إقلاع جديدة فيبقى التراجع بأمر واحد. وسطر في الكونسول يقول
متى كان آخر فحص.

**تمّ حين**: يظهر تحديث متاح في الكونسول، ويثبّته `hajimectl update`، ويعيد
`bectl activate` النظام إلى ما قبله.

---

## وخامس يجمعها: صورة التثبيت

الآلية موجودة في FreeBSD ولا نخترعها: ملف `/etc/installerconfig` على وسيط
الإقلاع يُنفَّذ تلقائياً ويعيد التشغيل وحده بعد انتهاء التثبيت.

**المطلوب**: صورة `.img` تُحرق على USB، تقلع، تسأل عن القرص والشبكة وكلمة
المرور إن لزم، ثم تبني كل ما سبق وتقلع إلى سطح مكتب مُثيَّم. تُبنى على جهاز
FreeBSD بـ`mkimg` و`makefs`، وصار عندنا واحد.

**تمّ حين**: شخص لا يعرف الطرفية يحرق ويقلع وينتظر، فيجد سطح مكتب. بلا سطر
واحد مكتوب.

الأربعة قبله شرط له: صورة تثبّت عنواناً ثابتاً أو تعريفاً خاطئاً تنشر العطب بدل
أن تحلّه.

---

## نواقص تخصّ فكرته هو

أقل إلحاحاً من الأربعة، وأكثر أثراً على من يستعمله فعلاً.

**النسخ الاحتياطي غير مجدول.** `hajimectl check` يقولها بنفسه:
`backup: no backup path configured`. نظام كل قيمته في قابلية التراجع ولا يأخذ
نسخة وحده. وهذا أقصر عمل وأكبر أثر في القائمة كلها.

**الدخول التلقائي بلا قفل شاشة.** كلمة مرور `hajime-desktop` مقفلة، لكن من يقف
أمام الجهاز يجد جلسة مفتوحة. مقبول لخادم في غرفة، غير مقبول كإعداد يُنشر.

**لا مراقبة.** `uptime-kuma` سقط ولم يحلّ محلّه شيء. والكونسول يقول ما يعمل
الآن ولا يقول ما انقطع الليلة الماضية.

**استعادة قواعد البيانات لم تُجرَّب.** أول تنفيذ سيكون على بيانات حقيقية.

**ثلاث حاويات بلا بديل ولا قرار**: `uptime-kuma` و`crowdsec` و
`telegram-openai-bot`، مسجّلة في `hajime-migrate/INVENTORY_2026-08-03.md`.

**`postgresql` و`redis` معلَّمان `Essential`** ولا صندوق في المشروع يستعمل
عميلاً لأيّهما. يحجزان 600 ميغابايت على جهاز ذاكرته ثمانية غيغابايت.

---

## ما يجب ألا يملكه

قائمة الرفض جزء من المنتج لا نقص فيه. أكثر ما تملكه توزيعات سطح المكتب يجعل
هذا النظام أثقل بلا أن يجعله أفضل فيما بُني له.

لا متجر تطبيقات، ولا واجهة رسومية لمدير الحزم، ولا طابعات ولا ماسحات، ولا
بلوتوث، ولا إدارة طاقة محمول، ولا سطح مكتب متعدد المستخدمين، ولا مثبّت بستّ
خطوات وأربعين لغة.

والصوت مثال قائم: العتاد مكتشَف (`Realtek ALC662`) ولا مكدّس صوت مُعدّاً، وهذا
صحيح ويبقى.

هذا خادم صغير يستبدل مكدّس حاويات على جهاز بثمانية غيغابايت. قول «لا» لما سبق
هو ما يبقيه كذلك.

---

## الترتيب

| # | العمل | لماذا هنا |
|---|---|---|
| 1 | النسخ الاحتياطي المجدول | أقصر عمل وأكبر أثر، ويحمي كل ما بعده |
| 2 | الشبكة: DHCP واكتشاف الواجهة | بدونه لا يعمل على جهاز ثانٍ أصلاً |
| 3 | اكتشاف كرت الشاشة | ثاني أسباب الشاشة السوداء |
| 4 | جدار `pf` مولَّد من جدول الخدمات | خادم مكشوف اليوم |
| 5 | `hajimectl update` وبيئة إقلاع لكل تحديث | لا أمان بلا ترقيع |
| 6 | صورة التثبيت | تجمع الخمسة، ولا تصحّ قبلها |
| 7 | قفل الشاشة، والمراقبة، والثلاثة بلا قرار | بعد أن يصير قابلاً للتوزيع |

وقبل النشر العالمي: الأربعة الأولى شرط، والسادس هو ما يجعله منتجاً.

---

## قاعدة تحكم كل ما سبق

كل بند هنا مكتوب معه **«تمّ حين»**، وهو شرط يُختبر لا شعور بالانتهاء. اليوم
علّمنا خمسة عشر عيباً أن ما لم يُشغَّل لا يُعرف: خط لم يُطبَّق سنة، ومدقّق
يعلن الأخضر على إعداد ساقط، ومثبّت ينتج حالة يرفضها كونسوله. لا شيء منها كان
ليظهر بالقراءة.

فلا يُشطب بند من هذه القائمة إلا بعد تشغيله على عتاد، والقياس بعده.
