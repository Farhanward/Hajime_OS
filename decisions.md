# decisions — Hajime OS

سجل القرارات: ماذا، لماذا، ومتى نراجعها.

---

## 2026-08-03 — طبقة العرض: Wayland بتركيب كامل

**القرار:** مركّب Wayland مكتوب من الصفر، بدون خادم X منفصل.
**السبب:** قرار فرحان الصريح. يريد نظاماً أصلياً لا طبقات مركّبة فوق بعضها.
**المراجعة:** بعد الجلسة 3 لو ثبت أن الطبقة غير ناضجة على FreeBSD.

## 2026-08-03 — الرسم: تكبير عددي من 640×360

**القرار:** الرسم الداخلي على 640×360 ثم تكبير ×3 بالجار الأقرب إلى 1920×1080.
**السبب:** يقلل الشغل الحسابي وحجم المخزن تسع مرات، ويحافظ على حدّة البكسل بدل ما يطمسها. مناسب للبيكسل آرت تحديداً.
**المراجعة:** لو احتجنا عناصر واجهة أدق من شبكة 640×360.

## 2026-08-03 — التسريع العتادي مؤجل

**القرار:** طبقة CRT وكل الرسم تُنفّذ بمسار برمجي أولاً، خلف واجهة تسمح باستبدالها بـ shader.
**السبب:** فرحان يريد `drm-kmod` لأداء السيرفر لا للزخرفة، ولاحقاً لا الآن.
**المراجعة:** عند تفعيل `drm-kmod`.

## 2026-08-03 — نطاق الاستبدال في v1: n8n فقط

**القرار:** نستبدل n8n فقط في الإصدار الأول. الواتساب والاستدلال يُؤجّلان.
**السبب:** فرحان لم يبدِ تفضيلاً فتُرك الاختيار لي. n8n أكبر مكسب ذاكرة بأقل مجهود. والأهم أننا لا نحتاج 100 بالمئة من مزايا n8n، نحتاج 100 بالمئة من الـ workflows التي تُشغَّل فعلاً، وهذا نطاق أصغر بكثير ويُجرَد في الجلسة 17.
**المراجعة:** بعد الجلسة 20.

## 2026-08-03 — تصحيح ترتيب الترحيل

**القرار:** النسخ الاحتياطي قبل المسح، والبناء على VM منفصل مع إبقاء السيرفر شغالاً حتى لحظة التحويل.
**السبب:** الترتيب الذي طرحه فرحان كان يضع مسح الجهاز في الخطوة الأولى والنسخ الاحتياطي في الثانية، وهذا يعني عدم بقاء شيء لنسخه. وهو في نفس الرسالة طلب انتقالاً بلا تعطيل، والترتيب المصحح هو ما يحقق ذلك فعلاً.
**المراجعة:** لا تُراجَع. النسخ قبل المسح شرط غير قابل للتفاوض.

## 2026-08-03 — استبدال n8n يمضي رغم أن المكسب في الذاكرة صغير

**القرار:** نكمل بناء بديل n8n بـ Rust.
**السبب:** الجرد أظهر أن n8n يستهلك 151 MB لا 350 MB، فالمكسب حوالي 130 MB فقط.
عُرض هذا على فرحان وقرر المضي لأن الهدف امتلاك أدوات أصلية، لا توفير الذاكرة وحده.
**المراجعة:** لا تُراجَع. الدافع ملكية الأدوات.

## 2026-08-03 — التوافق مع JSON الخاص بـ n8n على مستوى الشبكة

**القرار:** `model.rs` يطابق أسماء وأشكال حقول n8n حرفياً، بما فيها التداخل في
`connections` وبادئة `n8n-nodes-base.` في أنواع العقد.
**السبب:** يسمح باستيراد أي workflow مُصدَّر من n8n بدون خطوة تحويل، ويجعل
الترحيل قابلاً للتراجع: نفس الملف يشتغل على الاثنين.
**المراجعة:** عند إضافة عقد أصلية لا مقابل لها في n8n.

## 2026-08-03 — العقدة غير المنفَّذة تمرّر بدل أن تُفشل

**القرار:** أي نوع عقدة بلا منفّذ في السجل يمرّر مدخلاته كما هي.
**السبب:** يسمح بترحيل workflow عقدة بعقدة بدل دفعة واحدة. الـ workflow يصل
لنهايته حتى وهو مُنفَّذ جزئياً، فنقارن النتائج مع n8n مبكراً.
**المراجعة:** قبل الإنتاج نحتاج وضعاً صارماً يرفض العقد غير المعروفة.

## 2026-08-03 — النطاق النهائي للبرامج المخصصة

**القرار:** `release-api` و`telemetry-api` تُسقطان. `control-panel` يُعاد بناؤه
بـ Rust ليكون لوحة تحكم **النظام** لا لوحة موقع. `fatmalens` و`qood` تُسحب
كمشاريع مستقلة وتُعاد بعد تثبيت النظام، مثل موقع CarbonFlow.
**السبب:** قرار فرحان، وأيّده القياس: الخدمتان المُسقطتان سجّلتا **صفر طلب في
30 يوماً**، و`/vault/products` فارغ تماماً بلا ملف واحد منشور. بُنيتا ولم
تُستخدما. الإسقاط لا يفقد شيئاً عاملاً.
**المراجعة:** لو أُطلق منتج مكتبي يحتاج تحديثاً تلقائياً، يُعاد بناء
`release-api` بـ Rust حينها (335 سطراً فقط).

## 2026-08-03 — الواجهة: مُركِّب جاهز لا مكتوب من الصفر

**القرار:** `wayfire` من حزمة FreeBSD الجاهزة، مع `waybar` للشريط، ومدير ملفات
GTK، والتخصيص عبر CSS وأصول بيكسل. لا كتابة compositor.
**السبب:** البحث أظهر أن FreeBSD يشحن `wayfire 0.10.1` و`sway 1.12` كحزم
ثنائية. `wayfire` مكدّس النوافذ (يطابق مظهر إطارات CRT) وله نظام إضافات
وشيدرات، وهو المكان الطبيعي لطبقة خطوط الـ CRT. و`labwc` بديل بثيمات Openbox
تقبل صور PNG، لكنه بلا حزمة ثنائية. و`hikari` المصمم لـ FreeBSD **متوقف**.
**الأثر:** `hajime-wm` تحوّل من أثقل الأركان إلى أخفّها: تنسيق وأصول بصرية
بدل محرك عرض. أسابيع بدل أشهر.

## 2026-08-03 — اختبار جدوى Wayland: أداة تشخيص لا بوابة

**القرار:** `wayland_feasibility_test.sh` لم يعد حاجباً. يُستخدم وقت التثبيت
إن لم تظهر الشاشة.
**السبب:** تقرير موثّق لنفس جيل الرسوميات (Gen9.5) وصل إلى 1920x1080@60Hz بلا
إعداد X11. الحل حزمة `gpu-firmware-intel-kabylake`، والملف الناقص كان
`i915/kbl_dmc_ver1_04.bin`. وجهاز فرحان Kaby Lake أصلاً فالحزمة مطابقة حرفياً.
**ما يبقى غير مغطّى:** التقرير على 14.0 والهدف 14.2، وحزم `drm-kmod` المبنية
على 14.1 لا تعمل على 14.2. الحل معروف: إعادة البناء من المنافذ.

**وصفة التثبيت المعتمدة:**
```
pkg install drm-kmod gpu-firmware-intel-kabylake seatd wayfire wf-shell
sysrc kld_list="i915kms"
sysrc seatd_enable="YES"
```

## 2026-08-03 — الواجهة: مدير نوافذ لا كونسول ويب

**القرار:** يُبنى `hajime-wm` كمدير نوافذ Wayland كما في الخطة الأصلية.
**السبب:** فرحان اختاره بعد عرض بديل الكونسول الويب الذي يوفّر أشهراً بنفس
المظهر. القرار له.
**الأثر:** اختبار جدوى Wayland وDRM/KMS على FreeBSD (الجلسة 3) صار **حاجباً**
لا اختيارياً، لأن كل بناء الواجهة يقف عليه. لا يُبدأ بناء الواجهة قبله.

## 2026-08-03 — أربع قدرات نظام إضافية

**القرار:** تُبنى ضمن النظام: مدير حزم لخدمات Hajime، ولقطة ZFS تلقائية قبل
كل عملية خطرة، وفحص ذاتي عند الإقلاع (المنافذ والنفق والشهادات والنسخة
الاحتياطية) قبل إعلان الجاهزية، ووضع توفير يوقف غير الضروري ويُبقي المواقع.
**السبب:** اختيار فرحان من اقتراحاتي. الأربعة تخدم بنية النظام لا تشغيله فقط.

## 2026-08-03 — القدرات التي تلمس المضيف مبنية ومعطّلة افتراضياً

**القرار:** `executeCommand` و`ssh` و`readWriteFile` منفَّذة بالكامل، لكنها ترفض
العمل ما لم تُفعَّل صراحةً عبر `HAJIME_ALLOW_COMMAND` و`HAJIME_ALLOW_SSH` و
`HAJIME_FILE_ROOTS`.
**السبب:** فرحان طلب ألا ينقص شيء، وهذا منفَّذ. لكن عقدتَي `exec-cmd` و
`ssh-exec` معاملهما `"command": "={{ $json.body.cmd }}"`، أي تنفيذ ما يصل في
جسم الـ webhook. تشغيلهما افتراضياً يحوّل أي webhook مكشوف إلى صدفة على السيرفر.
المفتاح يجعل القرار واعياً لا عرضياً. والرفض صريح برسالة تشرح كيف تُفعَّل، فلا
تمر العقدة بصمت.
**المراجعة:** عند الحاجة لتفعيلها، بعد التأكد أن لا webhook مكشوفاً يصلها.

## 2026-08-03 — بصمة مفتاح المضيف إلزامية في عقدة ssh

**القرار:** `HAJIME_SSH_FINGERPRINT` مطلوبة، وبدونها العقدة ترفض العمل.
**السبب:** قبول أي مفتاح يجيب يفتح اعتراضاً في المنتصف على قناة تُنفَّذ عبرها
أوامر ذات صلاحية. والاعتمادات تأتي من الإعداد لا من ملف الـ workflow، فالتصدير
يبقى خالياً من الأسرار.
**المراجعة:** لا تُراجَع.

## 2026-08-03 — سجل التنفيذ لا يحفظ محتوى العناصر

**القرار:** `history.jsonl` يسجّل النتيجة والمدة والعقدة الفاشلة فقط، لا
حمولات العناصر.
**السبب:** العناصر تحمل روتينياً اعتمادات ومحتوى صفحات. ملف سجل بصلاحيات قراءة
واسعة ليس مكانها. المعلومة التشغيلية تبقى كاملة بدونها.
**المراجعة:** لو احتجنا تتبّعاً أعمق، يكون في مخزن منفصل بصلاحيات أضيق.

## 2026-08-03 — الصدق في واجهات الحالة

**القرار:** أي endpoint لا ينفّذ وظيفته يجب أن يعلن ذلك صراحة، ولا يرجّع `status: connected` أو قائمة نماذج مدعومة وهو لا يدعمها.
**السبب:** الكود الحالي يعلن قدرات غير موجودة، وهذا يخدع المطوّر بعد أشهر ويؤدي لقرارات نشر خاطئة. نفس المشكلة في `restore_data.sh` الذي يطبع نجاحاً مطلقاً مهما فشل.
**المراجعة:** لا تُراجَع.

## 2026-08-03 — نسخة FreeBSD المستهدفة: 14.4 لا 14.2

**القرار:** الهدف **FreeBSD 14.4**.
**السبب:** 14.2 المذكورة في الخطة الأصلية **منتهية الدعم**، و14.0 منتهية منذ
أكتوبر 2024، و14.3 انتهت في يونيو 2026. المدعومة حالياً 14.4 حتى نهاية 2026.
تشغيل نسخة بلا ترقيعات أمنية على سيرفر يستضيف متجراً ببيانات عملاء ومواقع
عامة خلف نفق مكشوف مقايضة خاسرة، مهما كان مكسب توافق كرت الشاشة.
**فائدة جانبية:** عائق `drm-kmod` على 14.2 كان عدم تطابق تغليف (حزم مبنية على
14.1)، لا جداراً عتادياً. على 14.4 المدعومة تُبنى الحزم لها فعلاً فيختفي
العائق من أصله.
**المراجعة:** عند اقتراب نهاية دعم 14.4 (ديسمبر 2026)، يُنظر في 15.x.

## 2026-08-03 — استخدام المنفذ الجامع `drm-kmod` لا نسخة مثبّتة يدوياً

**القرار:** `pkg install drm-kmod` لا `drm-61-kmod`.
**السبب:** المرجع الرسمي لمشروع سطح مكتب FreeBSD يوضح أن `drm-kmod` منفذ جامع
يختار نسخة المشغّل المطابقة للنظام تلقائياً. تثبيت نسخة بعينها يدوياً هو
بالضبط ما يُنتج عدم التطابق الذي رأيناه في تقارير 14.2.
**تأكيد جانبي:** شرط العتاد "Haswell أو أحدث"، وكرت HD 630 من Kaby Lake أي
الجيل السابع، داخل النطاق بثلاثة أجيال.
**حدّ المرجع:** منشور في 2018، فلا يغطي مشكلة إصدارات 14.x الفرعية الحالية.

## 2026-08-03 — واجهة `hajime-ai`: صيغة OpenAI لا صيغة Ollama

**القرار:** `/v1/chat/completions` هي الواجهة المعتمدة. عقد Ollama
(`/api/chat`, `/api/tags`) يبقى غلافاً للتوافق أثناء الترحيل فقط.
**السبب:** فرحان حرّرني من التقيّد بالعقد القائم. صيغة OpenAI هي المعيار
الفعلي الذي تفهمه كل مكتبة عميل، وتحمل استدعاء الأدوات كمفهوم أصيل لا كعُرف
في البرومبت. عقد Ollama ملكية ويحمل الأدوات بشكل أضعف.
**المراجعة:** تُحذف مسارات Ollama بعد ترحيل آخر خدمة تستخدمها.

## 2026-08-03 — بوابة الأدوات: نقطة اختناق واحدة

**القرار:** كل نداء أداة يمر بـ `tools::Gateway` الذي يفرض الميزانية والقاطع
ووضع الظل والتدقيق.
**السبب:** حدود موزّعة على كل خادم أداة تتباعد وتصير زينة. نقطة واحدة تعني
قاعدة واحدة تُختبر مرة.
**تفصيلة مقصودة:** الأداة التي لا تعلن `effect` تُعامل **خارجية**، فالنسيان
يؤدي إلى حماية أكثر لا أقل. ووضع الظل **مفعَّل افتراضياً**
(`HAJIME_AI_LIVE=1` لإطفائه)، لأن نشراً جديداً يجب أن يصف ما سيرسله قبل أن
يُؤتمن على إرساله.
**المراجعة:** لا تُراجَع.

## 2026-08-03 — أدواتنا تُربط داخل العملية، ونقل MCP للأدوات الخارجية

**القرار:** `hajime-social` وأمثاله يُنفّذون `ToolProvider` ويُربطون داخل
`hajime-ai` مباشرة. وصف الأدوات يبقى بشكل MCP (`name`, `description`,
`inputSchema`) فالنموذج يراها كأي أداة MCP.
**السبب:** قيمة نقل MCP في عزل الأدوات التي **لم نكتبها**. لأدواتنا، عملية
مستقلة لكل قدرة تكلّف ذاكرة على جهاز بـ 8 جيجا لا يملك فائضاً، وتضيف قفزة
IPC بلا مقابل. تجربة النموذج واحدة في الحالتين لأن الوصف واحد.
**المراجعة:** عند إضافة أداة من طرف ثالث، تُوصل عبر نقل MCP حقيقي.

## 2026-08-03 — الأداة تُعرض فقط إن كان سرّها موجوداً

**القرار:** `Social::from_secrets` لا يسجّل منصة بلا اعتماد. النموذج لا يرى
إلا الأدوات التي تعمل فعلاً.
**السبب:** أداة معروضة وهي مضمونة الفشل تُهدر ميزانية النداءات وتدفع النموذج
لإعادة المحاولة. الإخفاء أصدق من عرض ما لا يعمل.

## 2026-08-04 — التحقق على FreeBSD 14.4: عائقان لم يظهرا على ويندوز

بيئة اختبار: FreeBSD 14.4-RELEASE داخل QEMU داخل WSL2 (بلا تسريع عتادي،
لأن `/dev/kvm` يحتاج ويندوز 11). 8 أنوية، 4 جيجا، clang 19.1.7 في النظام
الأساسي، rustc 1.96.1 و go و git من الحزم.

**العائق الأول — `rquickjs` معكوس بين المنصتين.**
```
error: couldn't read .../rquickjs-sys-0.9.0/src/bindings/x86_64-unknown-freebsd.rs
warning: rquickjs probably doesn't ship bindings for `x86_64-unknown-freebsd`.
         try the `bindgen` feature instead.
```
على ويندوز عُطِّلت `bindgen` لغياب libclang. وعلى FreeBSD هي **مطلوبة** لأن
الحزمة لا تشحن ارتباطات لهذي المنصة. القرار السابق كان صحيحاً لبيئة التطوير
وخاطئاً للهدف، ولم يكن ليُكتشف بلا تشغيل فعلي.
**الحل:** اعتمادية خاصة بالمنصة في `hajime-workflow/Cargo.toml`:
```toml
[target.'cfg(target_os = "freebsd")'.dependencies]
rquickjs = { version = "0.9", features = ["bindgen"] }
```
مع `pkg install llvm` للحصول على `libclang.so`.

**العائق الثاني — نسخة whatsmeow في `go.mod` مختلقة.**
```
go.mau.fi/whatsmeow@v0.0.0-20240625083845-6acf596ff2a1: invalid version
```
كُتبت بلا تحقق لأن Go غير مثبت على جهاز التطوير.
**الحل:** تُحل النسخ بـ `go get ...@latest` لا بكتابتها يدوياً.

**ما نجح من أول مرة:** التحويل إلى rustls أثمر. شجرة الاعتماديات على FreeBSD
تُظهر `rustls` فقط، ولا أثر لـ `openssl-sys` ولا `native-tls`. لو بقينا على
native-tls لكان عائقاً ثالثاً.

**الدرس:** بناء ناجح على منصة التطوير لا يقول شيئاً عن منصة الهدف. كل ساعة
تأخير في هذا الاختبار كانت ستضاعف كلفة الإصلاح.

## 2026-08-04 — نتيجة التحقق الكامل على FreeBSD 14.4

**كل شيء يبني ويعمل.**

```
cargo build --workspace   Finished in 10m 48s
cargo test --workspace    178 passed, 0 failed
target/debug/hajime-workflow                      144M
hajime-wa-bridge  ELF 64-bit, for FreeBSD 14.4     23M
  GET /health          -> {"status":"SCAN_QR_CODE"}
  GET /session/default -> {"name":"default","status":"SCAN_QR_CODE"}
```

**أربعة عوائق، ولا واحد منها كان ليظهر على ويندوز:**

1. `rquickjs` لا يشحن ارتباطات لـ FreeBSD، فـ `bindgen` مطلوب هناك ومستحيل
   على ويندوز. الحل اعتمادية خاصة بالمنصة، مع `pkg install llvm` للحصول على
   `libclang.so`.
2. نسخة `whatsmeow` في `go.mod` كانت مختلقة. النسخ الآن محلولة بـ `go get`
   ومثبَّتة في المستودع مع `go.sum`.
3. واجهة `whatsmeow` تغيّرت: `sqlstore.New` و`GetFirstDevice` صارتا تطلبان
   `context.Context`.
4. `openssl-sys` تُجنِّب مسبقاً بالتحويل إلى rustls قبل الاختبار. شجرة
   الاعتماديات على FreeBSD تُظهر `rustls` فقط.

**الدرس المثبت:** بناء ناجح على منصة التطوير لا يقول شيئاً عن منصة الهدف.
الاختبار بعد خمسة صناديق كلّف ساعات؛ بعد عشرة كان سيكلّف أضعافها.

## 2026-08-04 — عقد الأدوات انتقل إلى النواة

**القرار:** `ToolSpec` و`ToolProvider` و`Gateway` انتقلت من `hajime-ai` إلى
`hajime-core`.
**السبب:** `hajime-social` يحتاج السمة لينفّذها، و`hajime-ai` يحتاج أن يركّب
`hajime-social`. بقاء العقد في `hajime-ai` كان يصنع دورة اعتماديات. والعقد
بطبيعته بنية تحتية مشتركة لا شيء يخص النموذج، فمكانه النواة.
**النتيجة:** الملف التنفيذي لـ `hajime-ai` صار **جذر التركيب**: المكان الوحيد
الذي يعرف أي مزوّدي أدوات موجودون. المكتبة تجهلهم، فإضافة قدرة لا تعني تعديل
البوابة.

## 2026-08-04 — `hajime-sys`: جدول خدمات واحد

**القرار:** جدول واحد في `service.rs` يعرف كل خدمة: رتبتها ومنفذها ومسار
فحصها وحجمها التقريبي. يقرأ منه `hajimectl` والفحص الذاتي معاً.
**السبب:** قائمتان تتباعدان، والتباعد لا يظهر إلا أثناء عطل.
**تفصيلة مقصودة:** الرتبة (`Essential` مقابل `Optional`) تحكم ثلاثة سلوكيات
دفعة واحدة: ترتيب الإيقاف، وما يوقفه وضع التوفير، وهل انقطاع الخدمة فشل أم
تحذير. اختبار يثبت أن النفق والبروكسي وقواعد البيانات ومحرك الـ workflow كلها
`Essential`، لأن تصنيف أحدها اختيارياً كان سيُسقط المواقع أثناء محاولة حمايتها.

## 2026-08-04 — الفحص الذاتي يفرّق بين الفشل والتحذير

**القرار:** ثلاث حالات لا اثنتان: `Pass` و`Warn` و`Fail`. الفشل وحده يمنع
إعلان الجاهزية.
**السبب:** خدمة اختيارية متوقفة ليست عطلاً، بل ما يفعله وضع التوفير عمداً.
لو عُوملت كفشل لتعلّم المشغّل تجاهل الاثنين. والعرض يضع الفشل أولاً لأنه ما
يُقرأ فعلاً.

## 2026-08-04 — أسماء اللقطات تحمل سببها ووقتها

**القرار:** `hajime-<السبب>-<YYYYMMDD-HHMMSS>`.
**السبب:** ترتيب القائمة يصير ترتيباً زمنياً، والسبب مرئي بلا فتح شيء.
**حد مقصود:** سبب بالعربية ينهار إلى شرطات فيصير الاسم فارغاً. يُرفض صراحةً
بدل إنتاج `hajime--20260804`. مثبت باختبار.

## 2026-08-04 — نهايات الأسطر: فخ لا يظهر إلا على المنصة الهدف

**العرض:** سكربتا `rc.d` الجديدان فشلا على FreeBSD بـ
`Syntax error: expecting "in"`، بينما مرّا على `sh -n` في Git Bash.
**السبب:** `write_text` في بايثون على ويندوز يحوّل `\n` إلى `\r\n`. الحرف
`\r` يلتصق بالكلمة المفتاحية `in` في جملة `case`، فيراها `sh` في FreeBSD رمزاً
مختلفاً. و`sh` في Git Bash يتسامح، فالخطأ يختفي على جهاز التطوير.
**الحل:** كتابة الملفات بـ `write_bytes` بنهايات `\n` صريحة، ومعها
`.gitattributes` يفرض `eol=lf` على كل السكربتات والمصادر.
**الدرس المتكرر:** هذا ثالث عيب في هذه الجلسة لا يظهر إلا بالتشغيل على المنصة
الهدف. رسالة الخطأ كانت تشير إلى السطر 63 بينما المشكلة في كل سطر.

## 2026-08-04 — `hajime-fetch`: طبقتان لا متصفح

**القرار:** جلب HTTP وتحليل HTML واستخلاص النص المقروء. لا تصيير ولا DOM.
**السبب:** ما يحتاجه النموذج نص المقال والعنوان والروابط، لا التخطيط ولا
الخطوط. هذا شغل تحليل لا تصيير، ويكلّف كيلوبايتات بدل مئات الميجابايتات.
**الحد المعلَن:** صفحة يبنيها JavaScript ترجع كقشرة فارغة. تُرفع بعلَم
`needsJavaScript` **ومعها جملة نصية** تخبر النموذج ألا يلخّصها كأنها فارغة،
لأنه يقرأ النثر أوثق مما يقرأ القيم المنطقية.

## 2026-08-04 — فحص العنوان قبل الاتصال، وثغرة IPv6

`fetch_page` هي الأداة الوحيدة التي يصير فيها برومبت سيئ **طلباً شبكياً**.
لذا يُفحص العنوان قبل فتح الاتصال: النطاقات الخاصة وloopback وعنوان بيانات
السحابة `169.254.169.254` كلها مرفوضة. وكل قفزة إعادة توجيه تُفحص من جديد،
لأن عنواناً مسموحاً قد يحوّل إلى عنوان داخلي.

**ثغرة أمسكها اختباري:** `http://[::1]/` كان **يمر**. الدالة تقسم جزء المضيف
على `:` لفصل المنفذ، وعنوان IPv6 بين قوسين مليء بالنقطتين، فصار المضيف `[`
ولم يُحلّل كعنوان أصلاً. الآن يُعالَج القوسان صراحةً، وقوس غير مغلق يُرفض بدل
أن يُخمَّن.

**تصنيف مقصود:** الجلب `Effect::Read` فيعمل في وضع الظل. بروفة يجب أن تستطيع
البحث حتى وهي ممنوعة من النشر.

## 2026-08-04 — `hajime-console`: تُرسَم على الخادم

**القرار:** الصفحة تُبنى في Rust وتُرسَل جاهزة، لا تُجمَّع بسكربتات في المتصفح.
**السبب:** لوحة تحتاج JavaScript لتخبرك أن النظام معطّل هي لوحة صامتة حين يهم.
**تفصيلة أمنية:** كل نص قادم من خدمة أخرى يُهرَّب قبل الإدراج. اسم الـ workflow
شيء كتبه إنسان ويعبر خدمتين قبل أن يصل. اختبار يُدخل وسم `script` كاسم workflow
ويتأكد أنه لا يصير markup.
**تفصيلة تشغيلية:** `Unreachable` منفصل عن `Error`. الأول يعني أن الخدمة لا
تعمل، والثاني أنها تعمل ورفضت. لوحة تعرض الاثنين كفراغ لا تعلّم المشغّل شيئاً،
فكل حالة تشرح نفسها بجملة.

## 2026-08-04 — `hajime-wm`: ثيم وإعداد لا محرك عرض

**القرار:** `wayfire.ini` مضبوط، و`hajime_theme.css` أُعيد كتابته بألوان الشعار،
و`install_desktop.sh` يثبّت ويتحقق.
**السبب:** wayfire يأتي كحزمة جاهزة على FreeBSD، ونظام إضافاته هو المكان
الطبيعي لطبقة خطوط الـ CRT. كتابة مُركِّب كانت ستكلّف أشهراً لنفس الصورة.
**استبدال الثيم:** القديم كان سماوياً وذهبياً على أسود مزرق، وهو تصوّر حاسوب
آخر عن الطرفية ويتعارض مع التميمة التي يفترض أن يجلس بجانبها. الجديد شاشة
كريمية في إطار خشبي دافئ بنص كحلي ولمسات وردية، مأخوذ من الشعار مباشرة.
**تفصيلة محفوظة:** زر «ابدأ» بقي من التصميم الأصلي، لكن بخطوط عربية حقيقية
مسمّاة أولاً في قائمة الخطوط، لأن الرجوع إلى monospace يكسر وصل الحروف.
**المثبّت يرفض بدل أن يخمّن:** نسخة منتهية الدعم توقفه، وكرت غير معروف يُحذَّر
منه صراحةً. تجربة جافة على الجهاز الافتراضي أثبتت ذلك: عرض كرت QEMU الوهمي
فحذّر أنه ليس Gen9.5 بدل أن يفترض.

## 2026-08-04 — المثبّت: ثلاث خصائص قبل السرعة

**قابل للتكرار.** التنويمات تُكتب ككتلة معلَّمة تُستبدل ولا تُضاف. النسخة
السابقة كانت تُلحِق بـ `loader.conf` بـ `cat >>`، فتشغيلها مرتين يضاعف كل سطر،
ومثبّت لا يُعاد تشغيله بعد فشل ليس مثبّتاً.

**قابل للتراجع.** بيئة إقلاع تُنشأ قبل أول تغيير. الجهاز بلا IPMI ولا وصول
للكونسول، فالطريق الوحيد للرجوع هو إقلاع على البيئة السابقة.

**صادق.** كل خطوة تفشل تقول ذلك وتتوقف. لا شيء ملفوف بـ `|| true` ليبقى
المخرَج أخضر.

**كل قيمة في الملف الذي يقرأها فعلاً.** المسوّدة الأولى وضعت `hw.re.tso`
و`hw.re.rxcsum` في `loader.conf`، وهذه التنويمات **غير موجودة** في `re(4)`
أصلاً. تفريغ الحمل يُضبط بأعلام `ifconfig` لكل واجهة. وكذلك `dev.cpu.0.cx_lowest`
انتقل إلى `power_profile` لأن القيمة الخام تُعاد كتابتها عند كل تبديل ملف طاقة،
و`sysctl.conf` يُنفَّذ قبل أن تكون أجهزة المعالج قد ارتبطت بالضرورة.

**TSO مُطفأ والمجاميع مفعّلة.** لـ `re(4)` تاريخ طويل في إفساد المقاطع الكبيرة،
وهذا المضيف ينهي TLS.

**التجربة الجافة لا تتوقف عند أول عائق.** `die` في وضع التجربة يسجّل ويكمل،
فالغرض من التجربة رؤية الخطة كاملة. توقُّفٌ عند أول مشكلة يخفي الأربع خلفها.

## 2026-08-04 — الاستعادة: التحقق قبل الكتابة

**السكربت الذي حلّ محلّه كان ينهي كل أمر بـ `|| true` ثم يطبع «0 Data Loss».**
هذا أسوأ عطل متاح لأداة استعادة: لا تستطيع أن تخبرك أن قواعد البيانات فارغة،
فتكتشف ذلك لاحقاً والجهاز المصدر ممسوح.

**المجاميع أولاً.** استعادة من نسخة مبتورة أسوأ من عدم الاستعادة لأنها تبدو
ناجحة. لا تُكتب بايت واحدة قبل أن تُطابق `SHA256SUMS`.

**`ON_ERROR_STOP=1` مع psql.** بدونها يُبلّغ psql بالنجاح بعد تخطّي كل جملة
فاشلة، وهكذا تنتج الاستعادة قاعدة فارغة بصمت.

**قواعد موجودة مسبقاً تُرفض بلا `--force`.** الكتابة فوقها بلا سؤال ليست
استعادة.

**الوركفلوهات تُفحص بالمحرّك الذي سيشغّلها** عبر `hajime-workflow --validate`.
ملف نُسخ ولا يُحلَّل هو ملف لم يُستعَد.

**مجلدات دوكر تُفكَّك في منطقة انتظار ولا تُسمّى «مستعادة».** هي تصف مكدّساً لم
يعد موجوداً، وتسميتها استعادة كذب.

## 2026-08-04 — jails: التراجع لخدمة واحدة

**ثلاثة jails لا واحد لكل خدمة.** الـ jail يكلّف مجموعة بيانات ومدخل توجيه،
لكن فصل خدمتين تتشاركان مقبساً يكلّف قفزة شبكة وجلسة تنقيح. التقسيم يتبع درجة
الانكشاف: `web` يواجه الإنترنت ويشغّل كود غيرنا، `data` لا يُبلَغ من الخارج،
`ai` يشغّل النموذج.

**السبب ليس العزل بحد ذاته** بل أن كل jail على مجموعة بيانات خاصة، فتُلتقط
وتُحدَّث وتُرجَع وحدها. بيئة الإقلاع تُرجع الجهاز كله، وهذا يُرجع النموذج وحده.

**بلا Bastille.** `jail.conf` الأصلي و`bectl` يفعلان ما نحتاجه لثلاثة jails
ثابتة، وBastille اعتماد إضافي.

**بلا VNET.** مكدّس شبكة كامل لكل jail يكلّف ذاكرة وإعداد توجيه، ولا شيء هنا
يحتاج جدار ناره الخاص. عناوين بديلة على `lo1` تكفي.

**النموذج لا يصل للخارج بنفسه.** قاعدة pf تمنع الخروج من `ai` إلى غير شبكة
الـ jails. البوابة على المضيف هي التي تجلب، وهي التي تكتب ما جلبت.

**قواعد pf تُكتب في ملف جانبي لا في `/etc/pf.conf`.** مجموعة قواعد مولَّدة
تستبدل قواعدك هي الطريقة التي يفقد بها جهاز وصوله البعيد.

## 2026-08-04 — الثغرات الثلاث المؤجَّلة

**التشغيلات الفائتة.** كانت الحلقة تبدأ من `Utc::now()`، فخدمة توقفت ليلاً
تعود ظانّة أن شيئاً لم يحن، والوظائف المتخطّاة لا أثر لها في السجل ولا في
اللوحة. الآن تُكتب النبضة على القرص كل دقيقة، وعند الإقلاع يُحسب ما فات
ويُسجَّل في التاريخ كتشغيل فاشل ليظهر في اللوحة. **اللحاق مُطفأ افتراضياً**:
وظيفة يومية فاتت عطلة طويلة يجب ألّا تنطلق ثلاث مرات عند العودة.

**الكتابة ثم إعادة التسمية.** انهيار في منتصف الكتابة يترك النبضة القديمة، لا
ملفاً مبتوراً يُقرأ كـ«لا نبضة سابقة» فيخفي الفجوة.

**قفل مرة واحدة على الأكثر.** نسختان من الخدمة تعنيان تنفيذ كل جدول مرتين، ولا
شيء في أي منهما يلاحظ: كلتاهما تسجّل نجاحاً. الدليل الوحيد نشرٌ مكرَّر بعد
ساعات. استُخدم `flock` لا ملف pid: ملف الـ pid يجب أن يجيب «هل تلك العملية
حيّة» فيخمّن عبر pid قد يكون أُعيد استخدامه، ويبقى بعد الانهيار قفلاً عالقاً
يمنع إعادة التشغيل. القفل الاستشاري يحرّره النواة عند موت العملية كيفما ماتت.

**وضع الظل في المحرّك.** كان موجوداً في بوابة الأدوات فقط. الآن كل عقدة تعلن
أثرها، والافتراضي `External` أي الأخطر: نوع عقدة يُضاف لاحقاً ولا يُصنَّف
يُحجَز في تشغيل الظل بدل أن يمر. الأثر يُقرأ من العقدة لا من نوعها، لأن
`httpRequest` بـ GET يقرأ وبـ POST لا.

**أشد من بوابة الأدوات.** البوابة توقف `External` فقط. المحرّك يوقف كل ما ليس
`Read`، لأن بروفة تكتب ملفاً على القرص ليست بروفة.

**الرسم يكتمل رغم الحجز.** العقدة المحجوزة تُخرج عنصراً يصف ما كان سيحدث، فتكمل
بقية العقد ويبقى النص الكامل مرئياً. التوقف عندها يخفي كل ما بعدها، وهو معظم
سبب البروفة.

## 2026-08-04 — خطأ في ملفات pid كشفه الاختبار على الهدف

الثلاثة `rc.d` كانت تضع `pidfile` في `/var/run` مباشرة. لكن `rc.subr` يشغّل
الأمر عبر `su -m ${name}_user` (السطر 1163)، فـ `daemon(8)` ينفَّذ بلا صلاحيات،
و`/var/run` هو `drwxr-xr-x root:wheel`. النتيجة: الخدمة إمّا لا تقوم أو تقوم
بلا ملف pid، و`service ... stop` لا يجد ما يشير إليه.

نُقل إلى `/var/run/hajime/` يُنشأ في `prestart`، الذي يقيّمه `rc.subr` قبل
الـ `su` فيعمل بصلاحيات الجذر. تحقّقت من الأمرين على FreeBSD نفسه بدل افتراضهما:
قرأت `rc.subr` وجرّبت الكتابة بمستخدم غير مميّز.

ولنفس السبب انتقل ملف القفل من `/var/run` إلى `/vault/hajime/workflow.lock`،
وهو مجلد يملكه مستخدم الخدمة.

## 2026-08-04 — اختبار كان يسأل عن الجهاز لا عن الكود

`an_essential_service_that_is_down_fails_but_an_optional_one_warns` كان يفترض
أن المنفذين 1 و2 مغلقان، ثم يستنتج من ذلك أن قاعدة التصنيف صحيحة. مرّ على
ويندوز ومرّ على FreeBSD مرات، ثم **فشل مرة واحدة** على مضيف محمّل: عاد المنفذ 2
بـ `Pass`، أي أن شيئاً ردّ عليه في تلك اللحظة. ثلاث إعادات بعدها نجحت.

الخلل في تصميم الاختبار لا في الكود: كان يدّعي حقيقة عن الجهاز كله ليتحقق من
قاعدة لا علاقة لها بالشبكة. اختبار يفشل لسبب خارج ما يختبره يعلّم المرء تجاهل
الفشل، وهذه أسوأ عادة يمكن أن يكتسبها أحد من مجموعة اختبارات.

فُصلت القاعدة في `service_check(name, port, open, essential)` وتُختبَر الآن
بلا مقبس أصلاً وبالحالات الأربع كلها. وأُضيف اختبار منفصل لـ `port_open` يربط
مستمعاً حقيقياً على منفذ يختاره النظام (`port 0`)، يسأل، ثم يحرّره ويسأل مجدداً.
حتمي في الاتجاهين بدل التخمين برقم منخفض والأمل.

## 2026-08-05 — `hajime-model`: نموذج من الصفر، مجاله النظام نفسه

**التحفّظ أولاً:** تدريب نموذج لغوي **عام** من الصفر على i7-7700 بلا GPU وبـ 8
جيجابايت غير ممكن. التدريب المسبق يحتاج مليارات التوكنز وأسابيع GPU. لكن
المطلوب لم يكن عاماً: «وظيفته النظام و تركيبته». نموذج ضيق المجال يُدرَّب في
**عُشر ثانية** على المعالج نفسه، ووزنه 176 كيلوبايت.

**فكرة CAD طُبِّقت حرفياً.** في الـ CAD تُعلن الهندسة والقيود بينها، ويحافظ
الحلّال على اتساق التجميعة حين تتحرك قيمة. `world.rs` يفعل ذلك للخادم: الكيانات
خدمات وjails، والقيود «postgres قبل محرك الوركفلو» و«كل ما يعمل يجب أن يسع في
ثمانية جيجابايت»، والحلّال يجيب عمّا يفعله التغيير **قبل** أن يفعله أحد.

**النموذج لا ينحرف ولا يخترع.** الكيانات تُقرأ من نفس جداول `hajimectl`
والكونسول. ونسخة خاصة من قائمة الخدمات تصير خاطئة أول مرة تُعاد تسمية خدمة،
وخاطئة بصمت.

**القسمة بين المتعلَّم والقطعي هي التصميم.** الصياغة ضبابية فتُتعلَّم. أسماء
الخدمات ليست ضبابية: تأتي من الجدول، وتُطابَق حرفياً، ولا يملك النموذج طريقاً
لتسمية خدمة غير موجودة. اختبار يثبت ذلك بحقن `'; DROP TABLE services; --`
و`$(whoami)` وغيرها.

**خطّي لا عميق.** الفصل بين اثنتي عشرة نية مفرداتها بالكاد تتقاطع، من ثلاثة
آلاف جملة. النموذج الخطي يحلّها إلى سقف البيانات، ويُدرَّب في عُشر ثانية، ويسكن
في 176 كيلوبايت. شبكة أعمق كانت ستنفق ذاكرة أكثر لحفظ نفس المتن وتصعب مساءلتها.
**الحد الصادق:** لا يتعلّم ترتيب الكلمات. لا شيء في سطح الأوامر يعتمد على ذلك،
والمخطِّط يسأل بدل أن يخمّن حين يضيق الهامش.

**أخطاء حقيقية كشفها القياس، لا القراءة:**

*التدريب لم يكن يتدرّب.* النسخة الأولى تجمع التدرّج على المتن كله وتقسم الخطوة
على عدد الأمثلة، فعلى ألفَي جملة صار كل epoch يحرّك الأوزان بلا شيء تقريباً.
يعمل، لا يبلّغ بخطأ، ولا يستطيع ملاءمة متنه نفسه. الحل: SGD بتحديث لكل مثال،
مع خلط بذرة ثابتة (المتن مولَّد نية بنية، فالمرور بالترتيب يعني مئات تحديثات
`Start` متتالية ينقضها ما بعدها).

*التطبيع العربي ناقص.* «اطفئ» في المتن و«اطفي» عند الكتابة، والمطبِّع يطوي
أ→ا ولا يلمس ئ وؤ. فتعلّم النموذج الإملاء الذي صادف وجوده وقرأ الآخر نية أخرى.

*«ال» تخفي الاسم.* «البوستقرس» كلمة واحدة، وفحص حدود الكلمة رفضها. النتيجة
أسوأ شكل للفشل: النية صحيحة (`stop` بثقة 0.85) والمفعول به مفقود. أُضيفت
اللواصق العربية (ال، وال، بال، لل…) بقائمة محددة لا بقاعدة فضفاضة، وإلا عاد
«ai» يُطابق داخل «said».

*أمر jail لخدمة.* «خذ لقطة للكادي» كان يُخرج `hajimectl jail-snapshot caddy`،
وهو أمر يُحلَّل ويبدو معقولاً ويفشل عند التنفيذ: كادي خدمة لا jail. المستخرِج
يعرف الفرق ويحمله في `is_jail`، والمخطِّط كان يرميه.

**التلوّث الذي كشفه اختباري.** عند توسيع المتن نسختُ صياغات **من مجموعة
الاختبار إلى مجموعة التدريب**. الرقم قفز إلى 0.844 وكان كذباً. اختبار المطابقة
الحرفية في `corpus.rs` أمسك واحدة، والباقي أُزيل يدوياً. **الرقم الصادق 0.778**
على 45 جملة لم يرها النموذج، بأحد عشر صنفاً (التخمين 0.09).

**ما يهم أكثر من الدقة:** متوسط الثقة في الإصابات 0.67 وفي الأخطاء 0.43. عتبة
`CONFIDENT` تحوّل معظم الأخطاء إلى **سؤال** بدل فعل خاطئ. ولا يوجد في الأخطاء
اتجاه خطر: لا جملة تُقرأ `stop` وهي ليست كذلك بثقة عالية، و`stop` تسأل دائماً
قبل التنفيذ وتسمّي ما سيسقط معها.

**لا ينفّذ شيئاً.** `Plan` وصف. التشغيل قرار المستدعي عبر البوابة بضماناتها.
فصل القرار عن الفعل هو ما يجعل وضع الظل ذا معنى.

## 2026-08-05 — `execute.rs`: التنفيذ عبر البوابة لا عبر الصدفة

**البديل الأقصر كان أن يستدعي المخطِّط `hajimectl` مباشرة.** يعمل، وأقصر، ويعني
أن المكان الوحيد الذي يسجّل ما فعله النموذج لا يعلم أن شيئاً حدث. لوحة التدقيق
في الكونسول كانت ستعرض قائمة فارغة على جهاز ظلّ النموذج يوقف فيه خدمات طوال
بعد الظهر.

المرور بالبوابة يورّث ما تضمنه أصلاً: وسم الأثر لكل نداء، وميزانية، وسجل، وحجز
كامل في وضع التجربة.

**البدء موسوم `External` لا `Local`.** يغيّر ما يفعله الجهاز، ويُرى من خارجه.

**`enum` في مخطط الأداة يحصر الأسماء الممكنة.** المخطط هو ما يقرؤه أي نموذج
قبل اختيار وسيط. تركه مفتوحاً دعوة لاختراع معقول.

**الاسم يُفحص مرتين.** `slots` يضمن أن الأسماء من الجدول، والأداة تفحص مجدداً.
مقارنة نصية واحدة، وهي آخر ما يقف بين خطأ في أي مكان أعلى وبين سطر أوامر.
والأداة قابلة للنداء من أي شيء يملك البوابة، لا من المسار الذي مرّ بـ `plan` فقط.

**خطة تحتاج موافقة لا تُنفَّذ.** `Confirm` تحمل سؤالاً، وسؤال يجيب عنه من طرحه
ليس تأكيداً.

## 2026-08-05 — المثبّت يهيّئ عناقيد قواعد البيانات

postgresql بعد التثبيت مباشرة بلا مجلد بيانات، و`service postgresql start` عليه
يفشل برسالة عن initdb تُقرأ بسهولة كحزمة معطوبة. العنقود يُنشأ في المثبّت، فأول
تشغيل ينجح، وللاستعادة مكان تستعيد إليه.

`initdb` يرفض مجلداً غير فارغ، وهذا هو السلوك المطلوب: إعادة تهيئة صامتة فوق
عنقود قائم تدمّره.

mariadb يبني جداوله عند أول تشغيل فلا يحتاج خطوة مماثلة، لكنه يحتاج مجلد بيانات
يملكه.

## 2026-08-05 — درس تشغيلي من انهيار الجهاز الافتراضي

الجهاز أُطفئ بلا إغلاق نظيف، فعاد UFS بتضارب soft-update، ورفض الفحص التلقائي
إصلاحه، وسقط init إلى المستخدم الواحد ينتظر ضغطة مفتاح. من الخارج كان الشكل:
qemu حيّ بـ 2.8% معالج، ولا شيء يستمع على المنفذ 22، وSSH يعطي «انتهت المهلة
أثناء تبادل البانر». ثلاث علامات لا تقول أياً منها ما حدث فعلاً.

**ما كشفه:** لقطة شاشة عبر مقبس مراقبة qemu. النص كان صريحاً:
`UNEXPECTED SOFT UPDATE INCONSISTENCY; RUN fsck MANUALLY`.

**أخطائي في التشخيص، وكلاهما أطال الطريق:**

*حلقتا إعادة محاولة متنافستان* كانتا تفتحان اتصالات كل ثوانٍ، فتجاوزتا
`MaxStartups` في sshd، وصار الرد `Connection reset by peer` بدل انتهاء المهلة.
شخّصتُ الجهاز وكان الخلل في أدوات فحصي.

*أرسلتُ `quit` إلى مقبس مراقبة qemu* ظاناً أنه ينهي جلسة المراقبة. `quit` في
تلك الواجهة **ينهي qemu نفسه**. أطفأتُ الجهاز الذي كنت أشخّصه. إغلاق المقبس هو
ما ينهي الجلسة.

**الأثر على النظام نفسه:** لا شيء. لكنه تذكير بأن الجهاز الحقيقي على UFS…
ليس كذلك، هو على ZFS، وهذا بالضبط أحد أسباب اختيارها: انقطاع كهرباء على ZFS لا
يترك نظام ملفات يحتاج fsck يدوياً وحضوراً أمام الشاشة. الخادم بلا IPMI ولا
كونسول، فسقوطٌ إلى المستخدم الواحد عليه يعني رحلة إلى الغرفة.

## 2026-08-05 — أرشيف crate مبتور أوقف البناء

فشل البناء على FreeBSD برسالة:

```
failed to unpack package `rustix v1.1.4`
numeric field was not a number:  when getting cksum for
```

`rustix` جاء مع `fs4`، اعتماد القفل الذي أضفته لثغرة «مرة واحدة على الأكثر».
التنزيل انقطع حين قُتل الجهاز الافتراضي في منتصفه، وcargo لا يصلح هذه الحالة
من نفسه: يبلّغ بفشل فك الضغط ويتوقف.

**العلاج:** مسح `~/.cargo/registry/cache` و`src` كاملين لا الملف الواحد. إن
انقطع تنزيل واحد في تلك اللحظة فغيره على الأرجح انقطع معه، وملاحقتها فشلاً
بفشل تكلّف أكثر من إعادة الجلب.

**لماذا يستحق التوثيق:** ليس خطأً في الكود، لكنه يوقف البناء تماماً ورسالته لا
تشير إلى السبب (آلة أُطفئت أثناء التنزيل). ولولا البناء على المنصة الهدف لما
ظهر أصلاً.

## 2026-08-05 — `--skip-packages`، وحدّه

أُضيف علم يتخطّى مرحلة الحزم. مبرَّر لآلة تُدار حزمها بوسيلة أخرى: تثبيت بلا
إنترنت، أو صورة جاهزة، أو jail أساسه يحملها. **ولا يفحص وجودها**، فما يكسبه من
مرونة يدفعه لاحقاً بفشل أقل وضوحاً حين لا تكون موجودة، وهذا مكتوب في رأس الملف.

**والصراحة تقتضي:** أضفته وأنا أحتاجه للاختبار في جهاز بلا إنترنت. لكنه ليس علماً
اختُرع للاختبار وحده — الحالات الثلاث أعلاه حقيقية.

**خطوتا قواعد البيانات صارتا تتحققان من الحزمة أولاً.** بدون ذلك يفشل `initdb`
على آلة بلا postgres برسالة عن العنقود، لا عن الحزمة الغائبة.

## 2026-08-05 — كل ركن يعلن منفذه، بعد أن كان الكل على 5678

`Common::from_env` كان يرجع افتراضياً إلى `127.0.0.1:5678` **لكل** مستدعٍ.
هذا منفذ محرك الوركفلو، فالكونسول والبوابة وجسر واتساب كلها كانت تسقط عليه.
الكونسول يوثّق 8088 في رأس ملفه ويربط على 5678.

**كيف ظهر:** شغّلت الكونسول على الجهاز الافتراضي فأعاد صفر بايت على 8088.
تشغيله في المقدمة أظهر السطر: `listening on http://127.0.0.1:5678`. لم يكن
أي اختبار ليمسكه، لأن التوثيق والسلوك اختلفا بصمت ولا شيء يقارنهما.

**الأثر لو وصل للإنتاج:** على آلة يعمل فيها المحرك، الكونسول يفشل في الربط.
وعلى آلة المحرك فيها متوقف، **يجيب الكونسول على منفذ المحرك** — فيصل طلب
webhook إلى صفحة حالة.

**العلاج:** الافتراضي صار وسيطاً. سطر واحد في كل مستدعٍ، والمنفذ يجلس بجانب
الركن الذي يملكه. واختبار يثبت أن ركنين لا يقعان على منفذ واحد افتراضياً.

## 2026-08-05 — اختبار وظيفي كشف خطأين حقيقيين

**١. أسماء مجموعات البيانات كانت مثبَّتة على `zroot`.** مثبّت FreeBSD يسمّي
الحوض `zroot` افتراضياً، فالتثبيت صحيح على تركيب افتراضي و**خاطئ بصمت على أي
تركيب آخر**: كل jail يُبلَّغ عنه بـ«لم يُنشأ»، و`jail-snapshot` يرفض،
و`jail-rollback` لا يجد ما يرجع إليه.

ظهر لأن حوض الجهاز الافتراضي اسمه `hajimepool`. اللاحقة بقيت ثابتة
(`jails/web`)، والحوض يُقرأ من الآلة عبر `zpool list`، مع `HAJIME_POOL` لآلة
فيها أكثر من حوض حيث «الأول» تخمين لا جواب.

**٢. فحص المساحة كان يطلب حصة ثلاثة jails مهما طلبتَ.** `--only ai` كان يُرفض
على آلة تتسع لواحد. صار الفحص يتناسب مع العدد المطلوب فعلاً.

**ونتيجتان بدتا أخطاءً ولم تكونا:**

`the model.s view: no` في فحص لوحات الكونسول — النمط كان خاطئاً، لأن الفاصلة
العليا تُهرَّب إلى `&#39;` (خمسة محارف لا واحد). اللوحة موجودة، والتهريب يعمل،
والفحص هو الذي كان يكذب.

ورفض سكربت الاستعادة نسخةً «سليمة» — لأن نسختي الاصطناعية أدرجت `SHA256SUMS`
داخل قائمته الخاصة. `backup.sh` الحقيقي يكتبه في `/tmp` ثم ينقله، فلا يفحص
نفسه. الخلل في اختباري، والتحقق أثبت أنه يعمل.

## 2026-08-05 — مفتاح داخلي كان يتسرّب إلى رد الـ webhook

`respondToWebhook` يترك الرد المُجهَّز على عنصره الخارج تحت `__hajime_response`
لتجده طبقة HTTP. وذلك العنصر نفسه هو ما ترسله `responseMode: lastNode`، فكان
المتصل يستلم:

```json
{"__hajime_response":{"pong":true,"seen":1},"pong":true,"seen":1}
```

بياناته ومعها دفاتر Hajime.

n8n يقرن تلك العقدة بوضع `responseNode`، لكن الوركفلو قد يُوصَل بأي من
الوضعين، ولا ينبغي لأيٍّ منهما أن يسرّب داخلاً. الطبقة تنزع المفتاح قبل
الإرسال الآن.

**ظهر باستدعاء webhook حقيقي عبر `nc`.** لم تكن اختبارات الوحدة لتمسكه: كل واحد
منها يستدعي `take_response` التي تستخرج القيمة الصحيحة، ولا شيء فيها ينظر إلى
ما يُرسَل فعلاً على السلك.

## 2026-08-05 — اختبار كان صامتاً منذ كتابته

`cargo clippy` أبلغ عن سمة مكرَّرة في `render.rs` ودالة «غير مستعمَلة» بعدها.
السبب: إدراجي السابق ترك `#[test]` مرتين فوق اختبار، وسرقها من الذي يليه. فظلّ
`an_error_and_an_outage_render_differently` **لا يعمل أبداً** ويُحسب ضمن الملف
كأنه موجود.

عدّاد الاختبارات لم يكن لينبّه: رقم أقل بواحد لا يلفت نظر أحد. الذي أمسكه هو
`dead_code` بعد أن صارت البوابة تفشل على أي تحذير.

مسحتُ المشروع كله بحثاً عن حالات مشابهة (سمات مكرَّرة، ودوال داخل `mod tests`
بلا سمة): لا شيء آخر.

**البوابة الآن `-D warnings`،** والمشروع عند صفر تحذير. ذلك ما يجعل التحذير
الأول القادم هو الذي يُصلَح، بدل أن ينضم إلى ثلاثة وثلاثين لا يقرؤها أحد.

## 2026-08-05 — الإصلاح: حلّال يُثبت خطته قبل أن يعرضها

`plan` يجيب على طلب. `repair` يجيب على سؤال لا يُقال بصوت عالٍ: **ما الخطوات
التي تُصلح ما هو معطوب الآن، وبأي ترتيب.**

**الترتيب هو كل الصعوبة.** تشغيل المحرّك قبل postgres يتركه يعمل على قاعدة غير
موجودة، وهذا يُقرأ كأنه ناجح. وإيقاف mariadb لتحرير ذاكرة يُسقط Caddy والنفق
معه. قائمة الأعطال سهلة؛ **تسلسلٌ يحلّها بلا أن يخلق غيرها** هو ما يستحق الكتابة.

**الخطة تُحاكى قبل أن تُعرض.** كل خطوة تُطبَّق على نسخة من الحالة، والنتيجة
تُفحص ضد كل قيد. خطة لا تنتهي بجهاز سليم **لا تُعاد كخطة** بل كشرح لماذا لا
يُصلَح بتشغيل وإيقاف. الفرق يهم في الثالثة فجراً: «أربع خطوات وبعدها لا انتهاك»
شيء يُعمَل به، و«ينقصك 900 ميغابايت ولا ترتيب يكفي» يرسلك إلى حل آخر بدل دورة
مفرغة.

**الترتيب المطبَّق:** الذاكرة أولاً (البدء في جهاز ممتلئ هو كيف يبدأ التبديل)،
ثم ما يعتمد عليه غيره، ثم البقية. والإيقاف موسوم بأنه يحتاج موافقة؛ التشغيل لا.

## 2026-08-05 — الحجم: قِسْ قبل أن تُصغّر

الطلب كان «كل ما صغُر كان أفضل». فقستُ محورين بدل التخمين:

**سعة الجدول — عند حدّها الأدنى فعلاً.** كل تنصيف يكلّف دقة:

```
4096 → 0.778    1024 → 0.711    256 → 0.489
2048 → 0.733     512 → 0.533     64 → 0.422
```

عند 512 ينهار. فتصغير الجدول ليس مجانياً، و176 كيلوبايت هي الأرضية الصادقة
لهذه الدقة.

**الدقة العددية — مجانية تماماً.** ثمانية بتات لكل وزن تعطي **نفس 0.778**، بل
نفس الإجابة على كل جملة في مجموعة الاختبار (اختبار يؤكد التطابق الكامل لا
الرقم فقط). القرار مقارنة بين مجاميع، فتقريب كل وزن يزيح كل مجموع بقدر متقارب
ويبقى الترتيب.

```
32 بت  176K  →  0.778
 8 بت   44K  →  0.778
```

**الخلاصة:** أوزان أقل ليست مجانية، وبتات أقل مجانية. الملف على القرص من 388
كيلوبايت إلى 114.

**التوافق:** التحميل يقبل الصيغتين، فملف قديم يظل يعمل بدل أن يُعيد التدريب في
كل استدعاء.

## 2026-08-05 — اسم الخدمة ليس دائماً اسم سكربتها

`Service.name` كان موثَّقاً بأنه «اسم سكربت rc.d، وهو أيضاً كيف تُخاطَب على سطر
الأوامر». صحيح لكل خدمة إلا واحدة: منفذ MariaDB يركّب `mysql-server` بينما
متغيّره `mysql_enable`. فـ `service mysql onestatus` لا يجد شيئاً،
و**`hajimectl start mysql` كان سيفشل على الخادم**.

**كيف ظهر:** سطر واحد في `hajimectl status` داخل CI:

```
mysql   essential  3306   listening, not via rc
```

وهي الأداة تكون دقيقة: المنفذ مفتوح وrc لا يعرف الخدمة. الفكرتان كانتا حقلاً
واحداً، فصارتا حقلين: `name` لما يكتبه الإنسان، و`rc_script` حين يختلف.

**ولم يظهر إلا لأن الحزم رُكِّبت فعلاً في CI.** الجهاز الافتراضي بلا إنترنت لم
يركّب mariadb قط، فلم يصل إلى هذا السطر.

## 2026-08-05 — CI أمسك حزمة منتهية من أول تشغيل

`mariadb106` بلغت نهاية عمرها وأُزيلت من شجرة المنافذ، فصار
`pkg install mariadb106-server` يفشل. المثبّت كان سيموت عند تلك النقطة على
الخادم الحقيقي.

وهذا حرفياً ما كُتب في تعليق وظيفة FreeBSD: «لو تغيّر اسم حزمة أو أُعيدت
تسميتها، يفشل هنا لا على الخادم». **أمسكها في أول تشغيل.**

النسخة الآن `mariadb1011`، وهي LTS مدعومة حتى 2028، وتقرأ تفريغ 10.6 بلا تحويل.

## 2026-08-06 — المظهر: لوحة واحدة تصل إلى النواة

الطلب كان ثيماً وشاشة إقلاع وشعاراً وسبلاش، «في أصل النظام مثل ما مايكروسوفت
تضع ثيمها». الجزء الصعب لم يكن الرسم بل تحديد أين يعيش المظهر.

**اللوحة كانت مكتوبة بخط اليد في ثلاثة مواضع.** عشرون لوناً في
`hajime-console/static/console.css`، وعشرون في `hajime-wm/hajime_theme.css`،
وثلاثيات عشرية تقريبية في `wayfire.ini`. ثلاث نسخ من جدول واحد تعني أن أول
تعديل يجعل سطح المكتب والكونسول يختلفان على معنى الأحمر. صارت `palette.toml`
مصدراً واحداً، ويُطبع منه كل ما عداه.

**وأعمق ما وصلت إليه اللوحة هو `vt(4)`.** ستة عشر لوناً تُكتب في
`/boot/loader.conf`، فتقرأها النواة قبل أن تطبع سطرها الأول. كل رسالة بعدها —
من فحص الأجهزة إلى الانهيار — تخرج بألوان النظام لا بالألوان الافتراضية. هذه
هي الطبقة التي لا يستطيع أي سطح مكتب لمسها، وهي التي كان الطلب يقصدها.

**الماسكوت مكتوب بالكود، والأيقونات ليست كذلك.** الروبوت يظهر بستة مقاسات، من
مفضلة بستة عشر بكسلاً إلى ربع شاشة إقلاع؛ تكبير صورة واحدة يعطي عجينة وتصغيرها
يعطي ضجيجاً، وإعادة رسمه عند كل مقاس تعطي شكلاً صحيحاً في كلٍّ منها. أما أيقونات
الواجهة فمن Lucide وPhosphor: من يرسم أيقونات رسم «مجلد» أفضل مما سأرسمه، ولا
سبب لرسمها مرتين.

**ImageMagick كان يتجاهل الخطوط بصمت.** تمرير مسار ويندوز بشرطة عكسية إلى
`-font` لا يُفشل الأمر: يقرّر أن الوسيط اسم خط، لا يجد اسماً كهذا، ويرسم بخطه
الافتراضي. كل الأصول خرجت بخط خاطئ ولا شيء قال ذلك. الآن `check_fonts()` تقيس
ثمانية أحرف من Press Start 2P وتتوقع 128 بكسل بالضبط، فالفشل صار مسموعاً.

**لا شريط تقدّم في شاشة الإقلاع.** الصورة تُعرض بينما قائمة المُقلِع قائمة، وليس
في تلك اللحظة شيء يقيس تقدّماً. فحُملت الشاشة بما هو صحيح حينها: الإصدار،
والمستودع، والاسم، وطلب الدعم. أما الفحوص فتُطبع من `rc.d/hajime_splash` بعدها،
وكل سطر فيها جواب أمر: `bectl` لبيئة الإقلاع، و`hajimectl status` للخدمات. شاشة
تقول READY دائماً هي شاشة لا تُقرأ مرتين.

## 2026-08-06 — العربية إعداد نظام، لا نص مرسوم في صورة

النسخة الأولى من الشاشات حملت عربية مخبوزة: ترحيب على شاشة الإقلاع، وسطر تحت
الشعار، ولافتة على الخلفية. ثم جاء الاعتراض الصحيح: طرفية النواة لا تعرف
العربية، فما معنى نظام «يتكلم» العربية في صورة بينما طرفيته إنجليزية؟

`vt(4)` لا يشكّل الحروف ولا يعكس الاتجاه. عربية هناك تعني حروفاً منفصلة بترتيب
مقلوب، وهذا أسوأ من إنجليزية صريحة. فخرجت العربية من الرسوم ودخلت حيث تدخل في
كل نظام آخر:

- **صفّا دخول** في `/etc/login.conf`: `hajime-ar` و`hajime-en`، يضبطان `lang`
  و`charset` و`setenv`. الصدفة وسطح المكتب والتطبيقات ترثها كلها.
- **`hajime-lang ar|en`**: أمر واحد يبدّل صفّ المستخدم. يسري عند الدخول التالي،
  تماماً كما تفعل لغة العرض في ويندوز وماك.
- **`Name[ar]` في كل `.desktop`**: أسماء المشغّلات تُحلّ من اللغة وقت التشغيل.
- **الكونسول** يقرأ `?lang=` ثم `Accept-Language`، ويضبط `lang` و`dir` معاً.

`/etc/login.conf` قاعدة قدرات، ونقطتان زائدتان فيها تكلّف كل دخول على جهاز بلا
منفذ ثانٍ. فالمثبّت ينسخ الملف، ثم يكتب، ثم يشغّل `cap_mkdb`، وإن اعترضت يعيد
النسخة ويعيد البناء منها قبل أن يخرج.

**بقي استثناء واحد مقصود:** لوحة الإعلان في خلفية سطح المكتب تحمل سطراً عربياً.
لوحة مرسومة على مبنى ليست واجهة، ولا يقرؤها أحد بحثاً عن أمر.

## 2026-08-06 — ما الذي يُقارَن في CI وما الذي لا يُقارَن

الأصول المولَّدة المرفوعة تنحرف بصمت: يد تعدّل ملفاً مولَّداً، أو تعدّل المولّد
ولا تعيد تشغيله. الفحص الواضح هو إعادة التوليد والمقارنة بايتاً ببايت.

لكن ذلك يفشل على الصور لسبب لا يستطيع أحد إصلاحه: حروفها تُرسَّم بـ FreeType
الموجودة على الجهاز، ونسختان مختلفتان تضعان بكسلاً في مكانين. فحص يفشل في كل
تشغيل هو فحص يتوقف الناس عن قراءته.

فالقسمة: **النصوص تُقارَن بايتاً ببايت** — اللوحات، وLua الخاص بالمُقلِع، وفن
الطرفية، وثوابت Rust التي يترجمها الكونسول — وكلها مرسومة بهذا الكود وحده فتخرج
متطابقة على أي جهاز. **والصور تُفحَص وجوداً ومقاساً فقط**، وهو الجزء الذي يمكن
أن ينكسر فعلاً في التزام: صورة محذوفة أو بدقة خاطئة.

وفائدة جانبية: الفحص النصي لا يحتاج ImageMagick ولا يصيّر شيئاً، فيمر في ثوانٍ.

## 2026-08-06 — أربع ثغرات بين «يُقلع» و«يخدم»

الفحص قبل بروفة الاستعادة سأل سؤالاً واحداً: لو ثُبِّت هذا النظام على حديد
نظيف، ماذا يعمل؟ الجواب كشف أربعاً، اثنتان منها صامتتان تماماً.

**الكونسول لم يكن خدمة.** المثبّت يركّب الملف التنفيذي، ورسالة اليوم تشير إلى
`127.0.0.1:8088`، وسطح المكتب يفتح العنوان عند الدخول، و`hajime_splash` يطبعه
في نهاية الإقلاع. ولا سكربت `rc.d` له، ولا وجود له في جدول الخدمات. أي أن كل ما
سبق كان يشير إلى منفذ لا يجيب.

**وجسر واتساب كذلك.** `hajimectl` يديره كخدمة، و`hajime_wa` يفترض وجوده على
3001، ولا سكربت يشغّله. البوابة كانت ستقول «الجسر ساقط» إلى الأبد، وهي محقّة.

الاثنتان من نوع واحد: خدمة في الجدول بلا سكربت في الشجرة. فصار الاختبار يمنعهما
بدل التوثيق: `every_hajime_service_has_an_rc_script_in_this_repository` يقرأ
مجلدات `rc.d` الفعلية ويقارنها بالجدول. جُرِّب بحذف السكربت الجديد، ففشل وسمّى
الغائب.

**ولا إعداد لـ Caddy ولا لـ cloudflared.** الحزمتان تُركَّبان، وكلتاهما «أساسية»
في الجدول، ولا شيء يكتب لهما ماذا يخدمان. جهاز يمرّ بكل خطوات المثبّت بنجاح،
وتقول الحالة إن كل شيء يعمل، ولا يُفتح موقع واحد.

**ولا PHP.** المتجر مكتوب به، و`jail.rs` يصف الـweb jail بأنه «المواقع العامة
ومتجر PHP» منذ البداية، وقائمة الحزم لا تحوي مفسّراً.

### ما بُني للثالثة والرابعة

`hajime-web`: جدول مواقع واحد يولّد Caddyfile. المولّد يرفض ولا يخمّن — جدول
فارغ لا يُكتب منه شيء (ملف Caddy فارغ يعني بروكسي يجيب بصفحة بيضاء بلا سبب)،
ومجلد غائب يُرفض برقم سطره، وCaddy نفسه هو الحَكم الأخير فإن رفض الناتج أُعيد
الملف القديم.

وكل موقع يُكتب `http://` والـHTTPS التلقائي مُطفأ. ليس إهمالاً: Cloudflare ينهي
TLS عند حافته والنفق يحمل الطلب إلى هنا، فلا شيء يصل إلى المنفذ 80 من الإنترنت،
فلا يستطيع Caddy الإجابة على تحدي ACME. مطالبته بالمحاولة تعطي حلقة إعادة وموقعاً
معطلاً أثناءها.

أما معرّف النفق فلا يُكتب أصلاً. هو بيانات حساب، ومعرّف مخمَّن يعطي خدمة تعيد
المحاولة إلى الأبد وكل المواقع معطلة. القالب يشرح الأوامر الثلاثة التي تنتجه،
والمثبّت يحذّر ويكمل.

**وأسماء حزم PHP تحققتُ منها قبل كتابتها**، لأن اسماً خاطئاً يوقف المثبّت في
منتصفه: `php84` وامتداداته موجودة، و`php84-openssl` غير موجود لأن openssl مدمج.
والقائمة نفسها تُركَّب في وظيفة FreeBSD في CI، فإعادة تسمية في شجرة المنافذ تفشل
هناك لا على الخادم.

### ما لم يُصلَح

المولّد يعرف ثلاثة أنواع مواقع ولا يعرف نطاقاتك. `sites.conf` يُشحن فارغاً
بأمثلة، والمثبّت يتخطى خطوة البروكسي ويقول ذلك صراحة. هذا حد المعرفة لا حد
الجهد: كتابة نطاق مخترع في ملف إعداد أسوأ من تركه فارغاً.

## البروفة التي لم تكتمل، والمتصفّح

### تجاوز إثبات استعادة قواعد البيانات

بروفة الاستعادة على جهاز وهمي أثبتت الكثير: سلامة النسخة، ولقطة التراجع،
والأسرار، وأشجار المواقع، وredis. ولم تصل إلى PostgreSQL ولا MariaDB، لأن قرص
الجهاز الوهمي كان 4.5 GB مقابل 8 GB يشترطها المثبّت نفسه، فامتلأ القرص أثناء
`pkg`. أي أن `psql -f` و`mariadb --batch` على بيانات حقيقية لم يُنفَّذا مرة
واحدة.

القرار: نمضي دون إثباتهما.

هذا قرار صاحب المشروع لا استنتاج تقني، وأسجّله كما هو. الأساس الذي بُني عليه أن
النسخة الكاملة للخادم القديم صارت موجودة ومُتحقَّقاً منها: 34 GB فيها جدول
الأقسام وقطاع الإقلاع ونظام الملفات الجذري و`/vault` بكامله، وكل أرشيف اجتاز
`gzip -t` وفُتح وعُدّت مدخلاته. فلو فشلت استعادة قاعدة بيانات على الجهاز الحقيقي
فالمخرج ليس إصلاحها تحت الضغط، بل إعادة الخادم القديم كما كان.

ما يبقى معروفاً وغير محلول: أول مرة يُنفَّذ فيها ذلك المسار ستكون على الجهاز
الحقيقي وقت الهجرة. إن فشل هناك فسيفشل في أسوأ لحظة، وكلفته إعادة يوم كامل من
العمل لا خسارة بيانات. هذا هو ما اشتُري بتجاوز البروفة، وهو ما يجب أن يعرفه من
يقرأ هذا لاحقاً.

### المتصفّح: badwolf

مُشغّل الكونسول كان يستدعي `xdg-open` ولا معالج له، فالضغط عليه لا يفعل شيئاً.
المطلوب كان الأخف.

`netsurf` و`dillo` أخفّ فعلاً وكلاهما مرفوض: بلا محرّك JavaScript عامل، والكونسول
صفحة تعتمد عليه، فمتصفّح لا يشغّلها ليس خفيفاً بل غير مفيد. و`falkon`
و`qutebrowser` يجرّان Qt WebEngine أي Chromium كاملاً، على جهاز ذاكرته 8 GB
يخدم المواقع.

`www/badwolf` هو الوسط: واجهة بالحد الأدنى فوق WebKitGTK، فالمحرّك حقيقي والقشرة
حوله لا شيء تقريباً. تحققت من وجوده في شجرة المنافذ قبل كتابته هنا، على قاعدة
المستودع في أسماء الحزم، ووظيفة FreeBSD في CI تركّبه فإعادة تسمية تفشل هناك لا
على الخادم.

## قراران أُغلقا: مستقرّ المتجر، والكونسول على الإنترنت

### المتجر يسكن `/vault/litecart`

الصف كان معطَّلاً لأن هدفه القديم `127.0.0.1:80` يجعل Caddy يطلب نفسه. والجواب
لم يكن منفذاً بديلاً بل نوعاً مختلفاً: LiteCart بلغة PHP، فالصف `php` لا
`proxy`، وphp-fpm مثبّت لهذا الغرض منذ البداية.

المكان تحدّد بالفحص لا بالاصطلاح. الملفات لم تكن في `/opt` كما بدا، بل في حجم
Docker مُسمّى `carbonflow-tech_litecart_html` مُركَّباً على `/var/www/html`،
و`/vault/litecart` على الخادم القديم مجلد فارغ محجوز بالاسم. فذاك مستقرّه.

و`restore_data.sh` صار يضعه هناك بدل تركه في `restore-staging`. هذا استثناء
مقصود من قاعدة التجهيز: كل ما يُجهَّز هناك مادة للقراءة، والمتجر موقع يجب أن
يَخدم. وترك متجر حيّ في مجلد اسمه "restore-staging" هو كيف يصير المسار المؤقت
دائماً. الملكية `www` لأن Caddy وphp-fpm يعملان بها، وجذر مستندات لا يقرأه
الخادم يعطي 403 على كل صفحة.

ونتج عن هذا ترتيب مُلزم: المولّد يرفض جذراً غير موجود، فالمثبّت يحذّر في خطوته
ويكمل، وإعادة التوليد بعد الاستعادة هي ما يجعل الصف يعمل. مكتوب في الخطة عند
الخطوتين 4 و6.

### الكونسول لا يُنشر على نطاق عام

قاعدة واحدة في الإدخال تمرّر كل شيء إلى هذا الجهاز، فصفٌّ للكونسول يضع واجهة
إدارة على اسم عام بلا شيء أمامها. والقرار ألا يُنشر.

وهذا لا يسحب شيئاً كان الخادم القديم يقدّمه. `control-panel.carbonflows.store`
كان يخدم `carbonflow-control-panel`، تطبيقاً آخر يعمل منذ أربعة أسابيع، وتطابُق
منفذه 8088 مع منفذ الكونسول مصادفة قُرِئت ملاءمةً في أول اشتقاق للجدول.

البديل موجود ولا يكلّف شيئاً: `ssh -L 8088:127.0.0.1:8088` يترك الكونسول
محصوراً بمن يملك المفتاح أصلاً، أو ربطه بعنوان الشبكة المحلية عبر
`HAJIME_CONSOLE_BIND`. وإن احتاج نطاقاً عاماً يوماً فمكانه خلف Cloudflare Access
لا عارياً في جدول المواقع.

## 2026-08-09 — `hajime-wm`: الإقلاع يصل إلى سطح المكتب وحده

**القرار:** المثبّت الآن يضبط تسجيل دخول تلقائي على الطرفية الفعلية (`ttyv0`)
ويشغّل wayfire من صدفة الحساب نفسه، فرسالة "اكتب wayfire" اختفت من الرسالة
الختامية لأنها كانت الثغرة نفسها: شخص لا يستخدم الطرفية يصل إلى `login:` ولا
شيء بعده.

**الحساب:** `hajime-desktop`، حساب جديد بصدفة `/bin/sh` وكلمة مرور مقفلة (`pw
useradd -w no`)، لا `hajime`. الأخير حساب خدمة بصدفة `nologin` أنشأه
`install_hajime_os.sh`، ودخول تلقائي على صدفة لا تُفتح مطلقاً تصريح دون تنفيذ.
الاسمان يبقيان الفرق ظاهراً في `ps` وفي كل سجل: خدمات خلفية مقابل جلسة طرفية.

**الآلية:** إدخال `al=` في `/etc/gettytab` (وسم جديد `hajime-al` وليس تعديل
`al.Pc` الجاهز، لأن ذاك مخصّص لجذر النظام) وسطر `ttyv0` في `/etc/ttys` يشير
إليه — الأسلوب الموثّق في `man 5 gettytab` و`man 5 ttys`، لا تخمين. wayfire
يبدأ من حارس في `~/.profile`: يتحقق أن `$(tty)` هي `/dev/ttyv0` بالضبط (لا ssh
ولا `su`) وأن لا نسخة من wayfire تعمل أصلاً، ثم `exec wayfire` — بلا حلقة
إعادة محاولة مكتوبة هنا؛ صمام init(8) الخاص بإعادة تشغيل getty هو ما يمنع
حلقة تعطّل سريعة، لا كود مضاف.

**أربعة أعطال حقيقية أمسكتها التجربة اليدوية على الخادم، لا القراءة:**

1. اسم الحزمة: `gpu-firmware-intel-kabylake` غير موجود في مستودع
   FreeBSD-kmods؛ الاسم الصحيح `gpu-firmware-intel-kmod-kabylake` (بقطعة
   `-kmod-`). كان `pkg install` يرفض بصمت واضح، لكن كل نسخة سابقة من الاسم في
   الشيفرة والتعليقات كانت خاطئة أيضاً.
2. `install -d -o USER -m 755 target` في FreeBSD يملك الورقة الأخيرة فقط، لا
   المجلدات الأم التي أنشأها في الطريق. حساب جديد بلا `~/.config` سابق ينتهي
   بمجلد `.config` مملوك لـ root، فـ wf-panel وwf-background يفشلان في إنشاء
   مجلدهما الخاص تحته (`EPERM`) ويسقطان باستثناء غير معالَج على كل دخول — لا
   خلفية ولا شريط، أبداً. الإصلاح: تمليك `~/.config` نفسه صراحة قبل أي شيء
   يُبنى تحته. الثغرة نفسها كانت في `install_theme.sh` أيضاً.
3. `wayfire.ini` لم يفعّل إضافتي `wayfire-shell` و`foreign-toplevel`، فوِدجت
   `window-list` في اللوحة يعطّل نفسه بصمت تحذير واحد، ثم wf-panel ينهار
   بخطأ تجزئة إن شارك اللوحة أي وِدجت آخر بجانبه.
4. وِدجت `network` في `wf-shell.ini` يكلّم NetworkManager عبر D-Bus، وهذه
   خدمة لا توجد على FreeBSD أصلاً — الجهاز خادم بعنوان ثابت في `rc.conf`، لا
   حاسوب محمول يبدّل شبكات. الوِدجت يرمي استثناء Glib غير معالَج، ومع
   `window-list` يُسقط العملية كلها. أُزيل من `widgets_right`.

**عطل خامس اكتُشف أثناء العمل، لا يخصّ هذه المهمة وحدها:** `write_block()` في
كل من `install_desktop.sh` و`install_theme.sh` كانت تطبع الجسم والعلامة
الختامية بصيغة `%s%s` بلا فاصل، فحقل لا ينتهي بسطر جديد (وهذا شأن كل قيمة
من `$(...)`، لأن استبدال الأوامر يحذف كل سطر جديد لاحق) يترك العلامة ملتصقة
بآخر سطر فيه. أثر هذا فعلياً وبصمت في `/boot/loader.conf` و`/etc/motd.template`
على الخادم قبل أن يُكتشف — أُصلح في الدالتين وأُعيد تشغيل `install_theme.sh`
لتصحيح الملفين.

**التحقق:** إعادة إقلاع واحدة فعلية (لا محاكاة) أظهرت `i915kms` محمّلاً من
`kld_list`، و`login [pam]` ثم `wayfire` وwf-panel وwf-background تعمل باسم
`hajime-desktop` على `ttyv0` خلال أقل من نصف دقيقة من الإقلاع، بلا كتابة أي
حرف. كذلك سبقتها عدة تحقّقات حيّة عبر `kill -HUP 1` (يجعل init يعيد قراءة
`/etc/ttys` فيعيد تشغيل getty دون إعادة إقلاع كاملة) لضبط كل قطعة قبل تجربتها
على الإقلاع الحقيقي.

**ما تبقّى بشرياً:** حرق USB واختيار القرص والشبكة في المثبّت التفاعلي لم
يُغيَّر هنا؛ هذا يخص صورة تثبيت غير مُراقَبة مستقبلية، لا هذا المثبّت.
