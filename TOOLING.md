# أدوات النظام: البحث والقرارات

آخر تحديث: 2026-08-03
المصادر في آخر الملف.

---

## تصحيح قبل البدء

قلت سابقاً إن WAHA يشغّل Chromium. **هذا خطأ.** التحقق من السيرفر:

```
WHATSAPP_DEFAULT_ENGINE=NOWEB     محرك Baileys بالويب سوكِت
عمليات chromium داخل الحاوية: 0
الذاكرة: 166.9 MiB
```

الوسم `noweb` يعني بلا متصفح. و`cloakbrowser` يستهلك 5.2 MiB خاملاً.
فالمكسب من استبدال WAHA حوالي 147 MiB، لا إزالة متصفح.

---

## 1. بديل WAHA: whatsmeow

| | WAHA (NOWEB) | whatsmeow |
|---|---|---|
| اللغة | Node.js + Baileys | Go، ثنائي واحد |
| الذاكرة | 166.9 MiB مقيسة | 10 إلى 20 MiB |
| التشغيل | يحتاج بيئة Node | ثنائي أصلي، صورة صغيرة |
| الاستقرار | تسريبات ذاكرة موثَّقة في Baileys | لا زحف ذاكرة تحت أحمال طويلة |

**تحذير يخص الاثنين لا أحدهما:** whatsmeow عميل معكوس الهندسة وغير رسمي،
ويخالف شروط واتساب، وفيه حالات حظر موثَّقة في مايو 2025. هذا الخطر قائم
اليوم مع WAHA أيضاً لأنه من نفس الفئة، فالاستبدال لا يزيده ولا ينقصه. لكن
لا يصح أن يُبنى عليه دون علم.

**القرار:** جسر `whatsmeow` عبر عملية Go منفصلة يخاطبها `hajime-wa` عبر
HTTP. كتابة البروتوكول بـ Rust من الصفر أشهر بلا مقابل.

---

## 2. بديل Chromium: Lightpanda

مكتوب بـ Zig من الصفر. لا يرسم شيئاً: ينفّذ DOM وJavaScript عبر V8 والشبكة
فقط، ويعرض خادم Chrome DevTools Protocol.

- ذاكرة أقل بتسع مرات، وسرعة أعلى بإحدى عشرة مرة من Chrome
- متوافق مع Puppeteer وPlaywright عبر CDP، فالكود القائم يعمل بلا تغيير

**الحد الحقيقي:** تغطية واجهات الويب غير مكتملة، وبعض المواقع لن تسلك كما
تسلك في Chromium. لذا القرار: نجرّبه على مواقعك المستهدفة فعلاً قبل
الاعتماد، ونُبقي مساراً احتياطياً لمتصفح كامل للحالات التي يفشل فيها.

---

## 3. الإيميل الخاص: Stalwart

مكتوب بـ Rust. يستبدل Postfix وDovecot وSpamAssassin بعملية واحدة.

- SMTP وIMAP4rev2 وPOP3 وJMAP، ومعها CalDAV وCardDAV وWebDAV
- DKIM وSPF وDMARC وARC مدمجة، ومصنّف بريد مزعج إحصائي
- حوالي 100 MiB، ويعمل على 512 MiB. الستاكات التقليدية تحتاج 4 إلى 6 جيجا

هذا يغطي `support@carbonflows.store` الذي يمر اليوم عبر Resend، ويعطيك
استقلالاً عن مزوّد خارجي.

**تنبيه تشغيلي:** استضافة البريد بنفسك تعني إدارة السمعة وسجلات DNS
وقوائم الحظر. الأداة تحل الجانب البرمجي لا التشغيلي.

---

## 4. طبقة تعليم الأدوات: MCP

طلبتَ شيئاً متخصصاً في إخبار النموذج كيف يستخدم أدواته. هذا موجود ومعياري:
**Model Context Protocol**.

الخادم يعلن أدواته ومصادره عند الاتصال، والنموذج يكتشفها عبر `tools/list`
تلقائياً بلا برمجة خاصة لكل أداة. المواصفة المستقرة 2026-07-28، والـ SDK
الرسمي بـ Rust هو `rmcp`. وOllama وLM Studio يتحدثانه، فالنماذج المحلية
الصغيرة مشمولة.

**وهذا يعيد تشكيل المعمارية.** بدل أن يكون لكل قدرة عقد HTTP خاص، تصير كل
قدرة خادم MCP، و`hajime-ai` مضيفاً واحداً يجمعها. عقد واحد بدل خمسة.

---

## 5. عيون النموذج وأذناه

النموذج نصي، والأدوات تعطيه الحواس.

**السمع:** `whisper.cpp`. النموذج `tiny.en` بـ 75 MiB و`base.en` بـ 150 MiB،
والتشغيل المكمّم حوالي 388 MiB. يعمل بالوقت الحقيقي على معالج متواضع، ولـ
Rust ارتباطات جاهزة.

**البصر:** أوصي بالتدرّج. معظم ما تحتاجه فعلاً هو قراءة نص من صورة أو وثيقة،
وهذا يكفيه OCR بتكلفة عشرات الميجابايتات، لا نموذج رؤية بجيجابايتات. نبدأ
بـ OCR وتحليل بنية الصفحة، ونضيف نموذج رؤية لاحقاً إن ظهرت حاجة لا يغطيها.

---

## 6. حدّ يجب أن يُقال عن النموذج الصغير

بحثت في قدرة النماذج الصغيرة على استدعاء الأدوات. الخلاصة من قياسات 2026:

نماذج الـ 3B مناسبة للتصنيف والفرز، وغير موثوقة في الخطط متعددة الخطوات.
Phi-4-mini بـ 3.8B هو الخيار العملي الوحيد على جهاز بـ 8 جيجا.

يعني: نموذج صغير مع أدوات يعمل جيداً لو كانت المهمة «اقرأ هذا وصنّفه» أو
«نادِ هذه الأداة الواحدة». ويتعثّر في «ابحث ثم قارن ثم أرسل بريداً بالنتيجة».
هذا ليس عيب تنفيذ بل حد في حجم النموذج. الحل عند الحاجة: تصعيد المهام
المعقّدة إلى نموذج أكبر، لا انتظار أن يتحسّن الصغير.

---

## 7. ميزانية الذاكرة: المشكلة الحاسمة

السيرفر اليوم: 7.7 جيجا إجمالي، 3.5 مستخدمة، و**4.3 جيجا سواب مستخدمة
بالفعل**. أي أن النظام تجاوز ذاكرته الفعلية ورحّل صفحات للقرص قبل أن نضيف
شيئاً.

الإضافات المطلوبة:

| المكوّن | الذاكرة |
|---|---|
| نموذج 3B مكمّم | 2,000 إلى 2,500 MiB |
| Stalwart | 100 إلى 200 MiB |
| whisper.cpp أثناء التفريغ | ~400 MiB |
| Lightpanda | 50 إلى 100 MiB |
| خوادم MCP | ~100 MiB |
| `hajime-wa` | 20 MiB (يوفّر 147) |
| **الصافي المضاف** | **~2.7 إلى 3.3 جيجا** |

لا تركب. ولا مقدار من التحسين البرمجي يصنع 3 جيجا غير موجودة.

**التوصية:** i7-7700 يدعم حتى 64 جيجا DDR4. شريحتان بـ 16 جيجا أرخص وأسرع
من أي بديل هندسي، وتفتح كل ما سبق دون مساومة. بدونها لازم تختار: إما
النموذج المحلي، أو بقية الأدوات، لا الاثنين.

---

## 8. اقتراحات من عندي

**مخزن أسرار.** أسرارك اليوم في ملفات `.env` متفرقة، وتوكنات cloudflared
ظاهرة في مخرجات `ps` لأي مستخدم. قبل إضافة بريد ومتصفح وواتساب بصلاحيات
إرسال، يلزم مخزن واحد بصلاحيات ضيقة.

**حدود وقاطع دوران.** وكيل يملك متصفحاً وبريداً وواتساب يستطيع في حلقة
خاطئة أن يرسل مئة رسالة أو يستهلك حصة API كاملة. يلزم سقف لكل أداة وقاطع
يفصل عند التجاوز. هذا أهم من أي ميزة.

**سجل تدقيق للأفعال الخارجية.** `history.jsonl` يسجّل تشغيل الـ workflows.
الإرسال الفعلي (بريد، رسالة، طلب خارجي) يحتاج سجلاً منفصلاً يجيب: مَن أرسل
ماذا ومتى وبأي تفويض.

**تأكيد بشري للأفعال غير القابلة للتراجع.** إرسال بريد أو رسالة واتساب لا
يُسترجع. أقترح أن تمر هذه عبر طابور موافقة افتراضياً، مع إمكانية استثناء
مسارات موثوقة صراحةً.

**قياس قبل الاستبدال.** كل رقم في هذا الملف عن نظامك مقيس، وكل رقم عن أداة
خارجية منقول من مصدرها. لا نستبدل مكوّناً قبل قياس ما يستهلكه فعلاً، لأن
تقدير WAHA بـ Chromium كان خطأً كلّفنا افتراضاً كاملاً.

---

## المصادر

- whatsmeow: <https://github.com/tulir/whatsmeow>
- مقارنة whatsmeow وBaileys: <https://whatsapp.checkleaked.cc/blog/whatsmeow-go-whatsapp-libraries>
- Lightpanda: <https://linuxiac.com/lightpanda-promises-a-faster-lightweight-alternative-to-headless-chrome/>
- Lightpanda التفاصيل: <https://www.scrapingbee.com/blog/lightpanda-headless-browser/>
- Stalwart: <https://ajianaz.dev/stalwart-mail-server-the-rust-powered-all-in-one-email-platform-you-should-know/>
- مقارنة خوادم البريد: <https://profor.pro/blog/self-hosted-email-2026-mailcow-stalwart-mailu/>
- MCP Rust SDK: <https://github.com/modelcontextprotocol/rust-sdk>
- مواصفة MCP 2026-07-28: <https://blog.modelcontextprotocol.io/posts/2026-07-28/>
- whisper.cpp: <https://vexascribe.com/whisper-cpp>
- أحجام نماذج Whisper: <https://openwhispr.com/blog/whisper-model-sizes-explained>
- النماذج المحلية واستدعاء الأدوات: <https://www.promptquorum.com/power-local-llm/best-local-models-tool-calling-2026>
