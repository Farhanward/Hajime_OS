# معمارية Hajime OS وترتيب البناء

آخر تحديث: 2026-08-03

الغرض من هذا الملف: ترتيب بناء الأركان بحيث لا يتكرر الكود بينها، ولا يضطر
أي ركن لانتظار ركن آخر.

---

## القاعدة التي تمنع التداخل

خمس قواعد. كسر أي منها يعيد التداخل من الباب الخلفي.

1. **مكتبة مشتركة واحدة فقط.** كل ما يتكرر يعيش في `hajime-core`، ولا شيء
   يتكرر خارجها. لا توجد مكتبة مشتركة ثانية.
2. **الأركان لا تستورد بعضها.** `hajime-ai` لا يعرف `hajime-wa` كـ crate.
   التواصل عبر HTTP بعقد موثَّق فقط.
3. **كل ركن يملك تخزينه.** لا جداول مشتركة ولا ملفات حالة مشتركة. الالتقاء
   عند الحدود لا داخلها.
4. **مساحات أسماء للإعداد.** المشترك `HAJIME_*`، والخاص `HAJIME_<الركن>_*`.
   فلا يتنازع ركنان على متغيّر.
5. **واجهة تشغيل موحّدة.** كل ركن يعرض `/health` و`/status` بنفس الشكل، من
   `hajime-core`. فأدوات التشغيل تُكتب مرة واحدة.

---

## الطبقة صفر: `hajime-core`

**تُبنى أولاً. حاجب لكل ما بعدها.**

ليست كوداً جديداً. حوالي 890 سطراً مكتوبة ومختبَرة بالفعل داخل
`hajime-workflow` وتحتاج نقلاً:

| الوحدة | الأسطر | من يحتاجها |
|---|---|---|
| `history.rs` | 258 | كل ركن يشغّل شيئاً |
| `policy.rs` | 152 | كل ركن يلمس المضيف |
| `auth.rs` | 124 | كل ركن يعرض واجهة |
| `js` (من `nodes/code.rs`) | 353 | workflow، وربما ai لاحقاً |
| سقالة HTTP و`/health` | ~80 | كل ركن |
| قراءة الإعداد من البيئة | ~40 | كل ركن |

**لماذا أولاً:** `hajime-ai` و`hajime-wa` اليوم هيكلان يربطان على `0.0.0.0`
بـ `CorsLayer::permissive` وبلا مصادقة. أي بناء عليهما الآن يعني نسخ
`auth.rs` و`history.rs` مرتين، وبعد شهر تصير ثلاث نسخ متباعدة. النقل الآن
يكلّف جلسة، وتأجيله يكلّف إعادة كتابة.

**المخرَج:** crate مكتبة بلا ملف تنفيذي، و`hajime-workflow` يعتمد عليها
ويظل ناجحاً في اختباراته الـ 105.

---

## ترتيب الأركان

### 1. `hajime-wa` — بوابة واتساب

**لماذا أول ركن بعد النواة:**

- **أقل مخاطرة.** جلسة واتساب الحالية `qr_ready` أي غير مرتبطة، فالاستبدال
  لا يقطع خدمة عاملة. بخلاف Ollama الذي يخدم بوت تيليجرام حياً.
- **أكبر مكسب معماري.** WAHA يشغّل Chromium كاملاً لبروتوكول نصي. إزالته
  تشيل متصفحاً من السيرفر.
- **يختبر النواة بمستهلك ثانٍ.** أول استخدام حقيقي لـ `hajime-core` خارج
  `hajime-workflow` يكشف ما إذا كان التجريد صحيحاً، وهو أرخص اكتشاف الآن.
- **الكود الحالي هو الأخطر في المستودع.** `send_message` يرجّع
  `success: true` ومعرّف رسالة مفبرك بلا أي اتصال. من يبني عليه يظن أن
  رسائله تصل.

**الحدود:** الـ workflows تناديه عبر `httpRequest`. لا استيراد متبادل.

**القرار المعلّق:** جسر `whatsmeow` عبر عملية منفصلة، أو تنفيذ البروتوكول
بـ Rust. الأول أسابيع، الثاني أشهر.

### 2. `hajime-ai` — بوابة النماذج

**لماذا ثانياً:** حيّ ومستخدم، فاستبداله أخطر. ويستفيد من نضج النواة بعد
مستهلكين.

**البناء على مرحلتين، والتسمية صادقة في كليهما:**

المرحلة الأولى بوابة متوافقة مع OpenAI تمرّر إلى محرك استدلال قائم، مع
مصادقة وسجل وحدود معدّل. قيمتها في الحوكمة لا في الاستدلال، وتُعلن ذلك في
`/status`.

المرحلة الثانية استضافة الاستدلال داخلياً عبر `candle`. هنا تُحسم الحقيقة
التي تجاهلتها الخطة الأصلية: `qwen2.5:3b` يحتاج قرابة 2 جيجا مهما كانت
اللغة. رقم 15 ميجا يصف غلافاً لا محركاً.

### 3. `hajime-binary` — استخراج الوسائط

**قرار: مكتبة لا خدمة.** الخطة الأصلية تعدّه ركناً مستقلاً. لا قيمة تشغيلية
له وحده، ومَن يناديه هو `hajime-ai` فقط. خدمة مستقلة تعني قفزة HTTP
ومصادقة ومراقبة مقابل لا شيء.

يصير `hajime-core::media` أو crate مكتبة يعتمد عليها `hajime-ai`.

### 4. `hajime-wm` — مدير النوافذ

**أخيراً، وبفارق كبير.**

- **صفر قيمة للسيرفر.** بيئة رسومية على سيرفر بلا شاشة تحميل بلا مقابل.
- **أكبر مجهود على الإطلاق.** أشهر، مقابل أسابيع لكل ركن آخر.
- **يحتاج اختبار جدوى أولاً.** نضج مكتبات Rust للـ DRM/KMS على FreeBSD غير
  مؤكد. هذا هو `ج3` في `PLAN.md`، ولم يُنفَّذ بعد.
- **لا يتداخل مع شيء.** لا يشارك سطراً مع أركان السيرفر، فتأجيله لا يعطّل
  أحداً، وتقديمه لا يفيد أحداً.

---

## ما يبقى أداة جاهزة

بناء هذه من الصفر يزيد المخاطرة ولا يقلّلها:

| الوظيفة | الأداة | لماذا لا نبنيها |
|---|---|---|
| بروكسي عكسي وTLS | Caddy | تجديد الشهادات تلقائياً، وخطأ واحد في TLS يكشف كل نطاق |
| النفق | cloudflared | بروتوكول ملكية لـ Cloudflare، لا بديل |
| قواعد البيانات | PostgreSQL، MariaDB، Redis | عقود من اختبار المتانة |
| الحماية | CrowdSec | قيمته في قواعد بياناته المشتركة لا في كوده |
| المراقبة | uptime-kuma أو Prometheus | حل ناضج ورخيص |

---

## العقد بين الأركان

```
                    ┌──────────────┐
                    │ hajime-core  │  مكتبة: auth, policy,
                    │  (library)   │  history, http, js
                    └──────┬───────┘
             ┌─────────────┼─────────────┐
             │             │             │
     ┌───────▼──────┐ ┌────▼─────┐ ┌────▼──────┐
     │hajime-workflow│ │hajime-wa │ │ hajime-ai │
     │    :5678      │ │  :3000   │ │  :11434   │
     └───────┬───────┘ └────▲─────┘ └─────▲─────┘
             │              │             │
             └── HTTP ──────┴─────────────┘
```

`hajime-workflow` ينادي الاثنين عبر عقدة `httpRequest`. لا استيراد متبادل،
فأي ركن يُعاد كتابته دون لمس البقية.

`hajime-wm` خارج المخطط تماماً. لا سهم يصل إليه.

---

## طبقة الاحتواء: jails

ثلاثة jails على ثلاث مجموعات بيانات، بينها `lo1` وقواعد pf.

```
        المضيف: hajime-workflow، cloudflared، hajimectl، الكونسول
                              │
                       lo1 (10.99.0.0/24)
             ┌────────────────┼────────────────┐
      ┌──────▼──────┐  ┌──────▼──────┐  ┌──────▼──────┐
      │ web  .0.10  │  │ data .0.20  │  │  ai  .0.30  │
      │ caddy       │  │ postgresql  │  │ llamacpp    │
      │ litecart    │  │ mariadb     │  │ hajime_ai   │
      │             │  │ redis       │  │             │
      └─────────────┘  └──────▲──────┘  └──────┬──────┘
                              │                │
                     من web وai فقط      لا خروج للإنترنت
```

السبب ليس العزل بحد ذاته بل أن كل jail على مجموعة بيانات خاصة: تُلتقط وتُحدَّث
وتُرجَع وحدها. بيئة الإقلاع تُرجع الجهاز كله، وهذا يُرجع النموذج وحده.

ما يبقى على المضيف يبقى لأنه يحتاج المضيف: `hajime-workflow` يصل للجميع بحكم
تصميمه، و`cloudflared` يملك النفق، و`hajimectl` يدير الـ jails فلا يسكن داخل
واحد منها.

---

## البوابة القائمة

**التثبيت الفعلي على الخادم.** كل ما سبق مُختبَر على FreeBSD 14.4 داخل جهاز
افتراضي: السكربتات تُحلَّل، والمثبّت يعرض خطته كاملة، و`jail.conf` يُحلَّل
فعلاً، وكل الاختبارات تمر. لكن الخادم نفسه ما زال على Alpine. الجهاز بلا IPMI،
وFreeBSD لا تُثبَّت عبر SSH، فالخطوة التالية تحتاج وسائط تثبيت وحضور فرحان.

**بروفة الاستعادة** متجاوَزة بقرار فرحان. النسخة موجودة ومُتحقَّق من بصماتها،
والسكربت الذي يستعيدها صار يرفض العمل على نسخة لا تطابق مجاميعها، لكن لم يثبت
عملياً أنها تُستعاد.
