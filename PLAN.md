# Hajime OS — خطة العمل التنفيذية

آخر تحديث: 2026-08-03
الحالة: مسوّدة بانتظار الاعتماد

---

## 1. الهدف والنطاق

بناء بيئة FreeBSD أصلية على عتاد فرحان، تشمل:

- مدير نوافذ Wayland مركّب مكتوب من الصفر بـ Rust (`hajime-wm`)، برسم بيكسل آرت وطبقة خطوط CRT، بدقة 1920×1080.
- انترو إقلاع بسبرايتات بيكسل آرت بدل ملف الفيديو الحالي.
- خدمات Rust تستبدل أدوات مختارة من ستاك CarbonFlow الحالي، بنفس السلوك الخارجي.
- ترحيل كامل لبيانات السيرفر الحالي بدون فقدان وبدون انقطاع خدمة.

خارج النطاق في الإصدار الأول: استبدال محرك الاستدلال، استبدال Postiz، دعم XWayland الكامل.

---

## 2. القرارات المثبتة

| القرار | الاختيار | السبب |
|---|---|---|
| طبقة العرض | Wayland بتركيب كامل | قرار المستخدم. تركيب مدمج بدون خادم X منفصل |
| الرسم | سبرايتات بيكسل آرت + تكبير عددي | رسم داخلي 640×360 ثم ×3 إلى 1080p. يقلل الشغل تسع مرات ويحافظ على حدّة البكسل |
| طبقة CRT | مطلوبة | تُنفّذ كمسار برمجي أولاً، ومسار GPU لاحقاً عند تفعيل drm-kmod |
| التسريع العتادي | مؤجل | يُفعّل لاحقاً لأداء السيرفر. المسار البرمجي يشتغل بدونه |
| ترتيب البدء | إزالة المخاطر أولاً | أخطر افتراض هو نضج Wayland/DRM على FreeBSD. نختبره قبل أي بناء |
| نطاق الاستبدال v1 | n8n فقط | أكبر مكسب ذاكرة بأقل مجهود، ونتعلم منه للباقي |
| ذاكرة المشروع | نمط الأربع ملفات | README, STATUS, progress, decisions |

### تصحيح ترتيب الترحيل

الترتيب المعتمد يضع النسخ الاحتياطي قبل المسح، ويبني على جهاز منفصل بحيث يظل السيرفر شغالاً:

```
1. نسخ احتياطي كامل من السيرفر الحي + إثبات الاستعادة
2. بناء Hajime OS والتحقق منه على VM منفصل
3. تجربة استعادة بيانات الإنتاج على النظام الجديد داخل الـ VM
4. مسح السيرفر وتثبيت Hajime OS (بعد ثبوت الخطوات أعلاه)
5. استعادة البيانات + التحقق النهائي
```

---

## 3. المخاطر المعروفة

| # | الخطر | الأثر | التعامل |
|---|---|---|---|
| R1 | مكتبات Rust للـ DRM/libinput أقل نضجاً على FreeBSD منها على لينكس | يوقف مسار مدير النوافذ كاملاً | جلسة 3 مخصصة لاختباره قبل أي بناء |
| R2 | ذاكرة 8GB لا تكفي الستاك الكامل مع بيئة رسومية | تعطّل خدمات تحت الضغط | ميزانية ذاكرة في القسم 4، وقرار مبكر |
| R3 | تعديل حيّ داخل حاوية Postiz يضيع مع أي إعادة بناء | عطل LinkedIn بعد الترحيل | يُوثّق ويُعاد تطبيقه ضمن جلسة 26 |
| R4 | whatsmeow مكتبة Go، و`hajime-wa` مكتوب Rust | لا اتصال واتساب حقيقي | جسر عملية منفصلة أو FFI. قرار في جلسة 22 |
| R5 | خطوط بيكسل عربية نادرة، والعربية تحتاج تشكيل حروف | زر "ابدأ" والواجهة العربية | HarfBuzz + خط يُرسم يدوياً. جلسة 13 |
| R6 | 17 حاوية تُرحَّل من Docker إلى jails | فروقات شبكة وتخزين | تجربة كاملة في VM قبل الإنتاج |

---

## 4. ميزانية الذاكرة (1080p)

الأرقام تقديرية بانتظار قياس فعلي في جلسة 1.

| المكوّن | الذاكرة |
|---|---|
| قاعدة FreeBSD + النواة | 300 MB |
| ZFS ARC | 2048 MB (قابل للتخفيض إلى 1024) |
| `hajime-wm` (Wayland مركّب، 1080p) | 100 MB |
| PostgreSQL 17 | 500 MB |
| MariaDB 11.4 | 400 MB |
| Redis | 100 MB |
| Caddy + cloudflared + CrowdSec | 250 MB |
| `hajime-workflow` | 20 MB |
| `hajime-wa` | 40 MB |
| محرك الاستدلال (qwen2.5:3b مكمّم) | 2200 MB |
| Postiz + Temporal | 1000 MB |
| **المجموع** | **~6.9 GB من 8 GB** |

الهامش المتبقي أقل من جيجابايت، وهذا غير كافٍ لذاكرة التخزين المؤقت للملفات ولا لقفزات الحمل.

**توصية:** i7-7700 يدعم حتى 64GB DDR4. إضافة 16GB أرخص وأسرع حل من أي تحسين برمجي، وتفتح المشروع كاملاً بدون مساومات. بدونها لازم نختار: إما نسقط Postiz وTemporal، أو نسقط الاستدلال المحلي، أو نخفض ARC إلى 1GB ونقبل أداء ZFS أضعف.

---

## 5. تقسيم الجلسات

كل جلسة مصمّمة لتكون مستقلة، بمخرَج واحد واضح، وتنتهي بتحديث `STATUS.md` و`progress.md` حتى لا يضيع السياق.

### المرحلة 0: السلامة (جلستان)

**ج1 — جرد ونسخ احتياطي**
جرد كامل للسيرفر: الحاويات السبعة عشر، الأحجام، الشبكات، المتغيرات، المهام المجدولة، وقياس الذاكرة والقرص الفعلي. سكربت نسخ احتياطي يغطي قواعد البيانات (postiz, litecart, npm)، ومجلدات `/vault`، وأسرار `/opt/postiz/.env`، وتوكن Cloudflare، وملفات compose. مع بصمات تحقق.
المخرَج: `hajime-migrate/backup.sh` + تقرير جرد.

**ج2 — بروفة استعادة**
استعادة النسخة على VM نظيف وإثبات أن الخدمات تقوم. هذي بوابة: ما نكمل إلا بعد نجاحها.
المخرَج: تقرير استعادة موقّع بالنتائج.

### المرحلة 1: إزالة المخاطر (جلستان)

**ج3 — اختبار الجدوى**
FreeBSD 14.4 على VM، تثبيت Rust، ومحاولة رسم مستطيل ملوّن عبر DRM/KMS من Rust. فحص توفر `smithay` و`wlroots` و`seatd` و`libudev-devd` على FreeBSD.
المخرَج: صورة شاشة تثبت الرسم، أو تقرير فشل يوثّق العائق.

**ج4 — قرار الأساس**
بناءً على ج3: نبني فوق smithay، أو فوق wlroots عبر ربط C، أو نعيد النظر في المنصة.
المخرَج: قرار موثّق في `decisions.md`.

### المرحلة 2: النواة المشتركة (ثلاث جلسات)

**ج5** صيغة أطلس السبرايت، محمّل الأصول، اللون المفهرس ثمان بت.
**ج6** محرّك الرسم: نسخ السبرايت، التكبير العددي 640×360 إلى 1080p، تتبّع المناطق المتغيرة.
**ج7** طبقة خطوط CRT بمسار برمجي، مع واجهة تسمح باستبدالها بـ shader لاحقاً.

### المرحلة 3: مدير النوافذ (سبع جلسات)

**ج8** هيكل المركّب: مخرَج DRM/KMS، حلقة الأحداث، إدخال libinput.
**ج9** بروتوكول Wayland الأساسي: `wl_compositor`، `wl_surface`، `xdg_shell`.
**ج10** إدارة النوافذ: الترتيب، التركيز، توجيه لوحة المفاتيح والمؤشر.
**ج11** زخرفة بيكسل آرت: الإطار المعدني، الأزرار، العدادات.
**ج12** شريط المهام وقائمة ابدأ.
**ج13** النص العربي: HarfBuzz، خط بيكسل عربي، اتجاه RTL.
**ج14** توافق التطبيقات وقرار XWayland.

### المرحلة 4: الانترو (جلسة)

**ج15** تحويل `Hajime_OS.mp4` إلى أوراق سبرايت، ومشغّل مدمج في المركّب يشتغل عند الإقلاع. يلغي الاعتماد على mpv و ffmpeg.

### المرحلة 5: الخدمات (سبع جلسات)

**ج16** إصلاح خطأ الترجمة في `hajime-ai`، إغلاق الثغرات (ربط محلي، مصادقة، قائمة سماح لـ SSRF، حد لحجم التحميل)، تهيئة المستودع: git، README، `.gitignore`، اختبارات، CI.
**ج17** جرد الـ workflows الفعلية من n8n على السيرفر، وتحديد أنواع العقد المطلوبة فعلاً.
**ج18** منفّذ العقد في `hajime-workflow`: تنفيذ الرسم البياني، تمرير البيانات، معالجة الأخطاء.
**ج19** أنواع العقد المحددة في ج17: webhook، http، cron، شرط، تحويل.
**ج20** التخزين الدائم في PostgreSQL، والاستئناف بعد إعادة التشغيل.
**ج21** `hajime-ai`: بروكسي صريح لمحرك الاستدلال بدل ادعاء التنفيذ الذاتي. تقييم `candle` للاستضافة الداخلية.
**ج22** `hajime-wa`: جسر whatsmeow، أو تنفيذ بروتوكول multi-device. قرار موثّق.

### المرحلة 6: النظام والتكامل (خمس جلسات)

**ج23** سكربتات `rc.d` لكل الخدمات، مع بدء تلقائي فعلي عند الإقلاع.
**ج24** إعداد Bastille jails وقوالبها، وترحيل الحاويات إليها.
**ج25** إعادة كتابة المثبّت: قابل للتكرار، فحص صلاحيات، `pkg install` للاعتماديات، نسخ احتياطي لملفات النظام قبل تعديلها، وتراجع عند الفشل.
**ج26** نشر تجريبي على VM ببيانات إنتاج مستعادة. إعادة تطبيق تعديل Postiz الموثّق في R3.
**ج27** بروفة التحويل الكاملة مع خطة تراجع مكتوبة ومجرّبة.

### المرحلة 7: التحويل (جلسة)

**ج28** التحويل الفعلي على السيرفر، مع نافذة صيانة معلنة وخطة تراجع جاهزة.

---

## 6. بوابات التحقق

لا نتجاوز أي بوابة قبل تحقق شرطها:

| البوابة | الشرط |
|---|---|
| بعد ج2 | استعادة ناجحة ومثبتة على VM |
| بعد ج4 | إثبات أن الرسم يعمل على FreeBSD، أو قرار بديل موثّق |
| بعد ج14 | مدير النوافذ يشغّل تطبيقاً حقيقياً ويستقبل إدخالاً |
| بعد ج20 | `hajime-workflow` ينفّذ كل workflow مجرودة في ج17 بنفس نتيجة n8n |
| بعد ج27 | بروفة تحويل ناجحة مع تراجع مجرّب |
| قبل ج28 | موافقة صريحة من فرحان |

---

## 7. تقدير المدة

ثمانٍ وعشرون جلسة. الوتيرة تعتمد على حدود الاستخدام اليومي. جلسات المرحلة 3 أثقل من غيرها وقد تنقسم.

الأرقام تقديرات هندسية مبنية على برامج مشابهة، وتُراجَع بعد ج3 لأن نتيجة اختبار الجدوى تغيّر تقدير المرحلة 3 كاملة.
